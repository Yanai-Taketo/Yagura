# ADR-0008: フォワーダ配布キットの動的生成(管理 UI)

- 状態: accepted
- 日付: 2026-07-06
- 決定者: YANAI Taketo
- 関連: [ADR-0001](0001-project-founding.md)(スコープ)/ [ADR-0004](0004-security-model.md)(管理面の分離・監査・ACL)/ [ADR-0009](0009-architecture-support.md)(アーキテクチャ対応拡張・決定7・委任 #4)/ PR #99・#101(静的キットと実機検証の証跡)/ [利用者ガイド: Windows イベントログを Yagura へ転送する](../guides/forward-windows-eventlog.md)

## 文脈と課題

Windows 端末のイベントログを Yagura へ転送する手段として、Fluent Bit(Apache-2.0)の
プリコンフィグ配布キット(サイレント導入対応)を提供している(PR #99・#101。
Windows Server 2025 実機で MSI 無人導入 → サービス登録・遅延自動起動 → OS 再起動後の
転送再開・送信フレームの実測までの全経路を検証済み)。

静的キットの運用では、配布する管理者が **Yagura サーバの宛先(IP / ホスト名・ポート)を
自分で指定**する必要がある。企業配布基盤(Intune / SCCM / GPO)で数十〜数百台へ push する
場面では、この「宛先指定」が唯一の手作業であり、誤記した場合は**全端末が誤った宛先へ
送信し続ける**(UDP のため送信側にエラーも出ない)という失敗様式を持つ。

Yagura 管理 UI が「自機を指す設定を焼き込んだキット」を生成できれば、**手入力起因の**
宛先誤記を減らせる。ただしこれは失敗様式の**軽減であって除去ではない**——宛先候補から
どれを選ぶかの判断責任は管理者に残り、候補検出の誤りという新しい失敗様式も持ち込みうる
(「決定」の設計条件で対処)。また、配布後の運用中に宛先へ到達できなくなる事態
(経路変更・ファイアウォール変更・Yagura 停止等)は本 ADR の範囲外であり、
転送の死活監視は別途必要である(委任事項参照)。

なお、キットが導入する Fluent Bit サービスの実行アカウント(LocalSystem)は
第三者ツール側の既定であり、Yagura 本体のサービス実行権限方針(ADR-0004)の
対象外である。本 ADR はこの前提を変えない。

## 検討した選択肢

### 選択肢 A: 静的キットのみ(現状維持)

- 利点: 追加実装ゼロ。Yagura 本体の攻撃面・保守面が増えない
- 欠点: 宛先の手入力が残る(上記の失敗様式)。ガイド文書への往復が必須で、
  「Windows 管理者が深く意識せず導入できる」という体験目標に届かない
- 補足: 静的キットの試用フィードバックはまだ無く、「手入力誤記による導入失敗」の
  実測例も無い。それでも B へ進む判断は、実害の実測ではなく製品の体験目標
  (オーナー方針。v0.2 スコープ決定 2026-07-07)に基づくものであることを明記しておく

### 選択肢 B: 管理 UI での動的生成(採用案)

管理 UI(TCP 8515・loopback 固定 = ADR-0004)に「フォワーダキット生成」画面を追加する。
管理者は宛先・ポート・収集チャネルを指定し、置換済み設定一式を ZIP でダウンロードする。

- 利点: 手入力起因の宛先誤記を軽減。管理 UI 上の操作 1 回で「宛先設定込みの配布物」が
  得られる(**MSI の取得と配布基盤へのパッケージ化は引き続き別途必要**——全自動には
  ならないことを誇張しない)
- 欠点(コストの内訳):
  1. 宛先候補検出(NIC 列挙・除外・表示)の実装とテスト
  2. ZIP 生成(テンプレート置換・梱包)の実装とテスト
  3. テンプレートをビルド時に埋め込む工程がビルドパイプラインに増える
  4. Fluent Bit の版追従時、静的キットと生成 UI の**両方**を同時に更新する変更半径になる
     (テンプレート単一ソース化で緩和するが、検証責任は残る)
- Fluent Bit の MSI 本体は**既定では同梱しない**(取得手順の案内に留める。静的キットと
  同じ判断)。ただし管理者が明示的に配置した MSI に限り**オプトインで同梱できる**
  (2026-07-07 改訂。設計条件 9 と非同梱の判断根拠は改訂履歴 1 に一本化)

### 選択肢 C: 閲覧 UI(TCP 8514)での生成

- 却下: 閲覧 UI は認証なしで LAN に公開される(ADR-0004)。キット生成は読み取り専用操作
  ではあるが、「サーバの構成を反映した導入物の作成」は管理領域の操作であり、
  パラメータ付き生成エンドポイントを非認証面に増やすことは防御既定(閲覧面は読み取りのみ)
  と整合しない

## 決定

選択肢 B を採用する。管理 UI に「フォワーダキット生成」画面を追加し、
自機を指す設定入りの Fluent Bit 配布キット(ZIP)を生成・ダウンロードできるようにする。
MSI は既定で非同梱とし、管理者が明示配置した場合に限りオプトイン同梱を許す(設計条件 9)。

### スコープ境界の原則(ADR-0001 との整理)

ADR-0001 は「Windows イベントログ転送エージェントの自作」をスコープ外とし、その理由を
「既存の実績あるツールの設定例を案内する方が利用者の利益になる」とした。本 ADR は
この境界線を次の原則で明文化する:

> **Yagura が生成・保守するのは、既存テンプレートの実行時パラメータ化(置換)と梱包までとする。
> 転送ロジック本体・エージェント実行体(バイナリ・プラグイン・スクリプトの新規ロジック)には
> 関与しない。**

キット生成はこの原則の範囲内(= 設定例の案内の発展形)であり、原則を超える提案
(例: 転送エージェントの改造・独自プラグイン)は従来どおりスコープ外である。
この原則は将来の類似判断(他ツールの設定生成等)にも同じ基準で適用する。
境界判定の目安: **生成物の構造・ロジック(分岐)が入力パラメータの値によって変わらないこと**
——値の置換に留まれば「パラメータ化」、条件によって処理内容を組み替えるなら「ロジック生成」
であり原則の外(クリス round-2 の指摘を受けた判定基準)。MSI のオプトイン同梱は、
管理者が配置したファイルを**そのまま梱包する**だけで生成物のロジックを変えないため、
この原則の範囲内である。

### 設計条件

1. **宛先候補の提示**: サーバの NIC から検出した候補の選択式 + 手入力。候補からは
   ループバック・リンクローカル(APIPA 含む)・無効化された NIC を除外し、
   各候補に NIC の説明名を併記する。**既定選択は置かず、管理者に明示的に選択させる**
   (候補提示は誤記の軽減策であり、到達可能性の判断責任は管理者に残ることを画面上でも示す)
2. **収集チャネルの既定**: 静的キットと同じ `System,Application`。Security チャネルは
   **初期オフのオプトイン**とし、有効化時は機微情報・量の注意を表示する
3. **生成 ZIP の内容要件**: 置換済み `fluent-bit-yagura.conf` + `install.ps1` + Lua フィルタ +
   README + 生成メタデータ(`GENERATED.txt`: 生成日時・宛先・チャネル・Yagura バージョン)。
   README には最低限「検証済み Fluent Bit 版」「MSI は別途取得(取得元 URL と SHA256 検証手順)」
   「MSI 上書き更新後は install.ps1 を再実行」「宛先(サーバ IP)変更時は再生成・再配布が必要」を
   含める(静的キットガイドの注意事項を生成物側でも欠落させない)
4. **テンプレートの単一ソース**: 生成テンプレートは `forwarder/fluent-bit/` 配下のファイルを
   正とし、ビルド時に埋め込む(コピーを別に持たない)。具体機構は全体設計書へ委任
5. **置換値検証の単一ソース**: 宛先・ポート・チャネルの検証制約は `install.ps1` の
   `ValidatePattern` 定義を正とし、サーバ側実装との同期を自動テストで固定する
   (設定ファイルへの任意文字列注入を作らない)
6. **監査**: キット生成操作は ADR-0004 決定 7 の管理操作監査の対象に含める
   (生成日時・宛先値・チャネルを記録する。MSI 同梱時は設計条件 9 の追加項目も記録)
7. **生成の閉域性**: 生成はテンプレート置換と ZIP 化のみで、外部ネットワークへアクセスしない。
   ZIP は**メモリ上で組み立て、ディスクに一時ファイルを書かない**(ADR-0004 決定 5 の
   一時保管領域の論点を発生させない。MSI 同梱時は配置済みファイルを読み取って封入するのみ)
8. **秘密情報なし**: 生成物に秘密情報を含めない(現行の転送は平文 UDP・認証なしであり、
   キットには宛先とチャネル名しか入らない)
9. **MSI のオプトイン同梱**(2026-07-07 改訂。非同梱を既定とする判断根拠は改訂履歴 1):
   既定は非同梱を維持する。管理者がサーバの所定フォルダに Fluent Bit の MSI を手動配置した
   場合に限り、生成画面に「MSI を同梱する」チェックボックスをオプトインで表示し、生成 ZIP に
   MSI を含められる(ZIP 単体で自己完結する配布物になる)。次を満たすこと:
   - **配置フォルダと権限**: 配置先は `%ProgramData%\Yagura\forwarder\`(データルート配下)。
     **既定 ACL は Administrators のみ書き込み可**とし、緩めるインストーラ・手順は提供しない
     (ADR-0004 決定 5 の ACL 方針に準拠——ここに書ける者は「全端末に配る MSI を差し替え
     られる者」であり、防御上最も価値の高い書き込み対象になるため)。生成画面は**このパスを
     明示表示**し、フォルダに MSI が無い場合も「未検出であること + 配置先パス + 期待ファイル名」を
     案内する(「チェックボックスが出ない」だけの沈黙にしない)
   - **検出と版判定**: ファイル名パターン `fluent-bit-*-win64.msi` で検出し、版は **MSI の
     ProductVersion を優先**して判定する(ファイル名は補助。リネームで欺かれないため)。
     複数の MSI がフォルダにある場合は**既定でエラー停止**(生成を進めない)——安全側に倒し、
     管理者に単一化させる(自動選択の緩和は委任事項での再検討事項)
   - **版整合**: 同梱 MSI の版が「検証済み版」と異なる場合は生成画面で警告する。警告時の同梱は
     **単なる表示に留めず二段階確認**(管理者が明示的に承認する操作)を要し、警告の有無と承認の
     事実を監査記録に構造化して残す(「言った・言わない」を作らない)
   - **改ざんの事後確認(検知ではなく記録)**: 生成時にサーバが配置 MSI の SHA256 を計算し、
     `GENERATED.txt` と監査に記録する(生成画面にも表示)。これは配置**後**の同一性を管理者が
     事後確認するための記録であって、配置**前**に改ざんされた MSI を Yagura が検知する機構では
     ない。加えて、検証済み版に対応する公式配布の SHA256 を Yagura に静的に保持し、一致すれば
     「公式ハッシュと一致」・不一致なら明示警告する(自動照合の実現範囲は実装 PR で確定)。
     ZIP 全体の SHA256 も `GENERATED.txt` に併記し、配布経路での破損・改ざんを ZIP 単位でも
     確認できるようにする
   - **識別性**: 生成 ZIP のファイル名に同梱有無を機械可読に含める(例 `..._with-msi.zip` /
     `..._no-msi.zip`)。開かずに棚卸しできるようにする。想定 ZIP サイズが大きくなる(20 MB 超)
     ことは生成前に画面で予告する
   - **責任の所在(免責)**: README・`GENERATED.txt` に「同梱 MSI について Yagura は取得元・
     真正性・脆弱性対応の責任を負わない。Yagura は管理者が配置したファイルを梱包し、その来歴を
     記録するのみ」を明記する
   - 既定(非同梱)の全条件(README の取得手順・SHA256 検証手順の記載等)は同梱時も維持する

## 帰結

- 良くなること: 企業配布での導入手数が「MSI を置く → ZIP を展開して push」まで縮む。
  手入力起因の宛先誤記が減る。MSI オプトイン同梱で ZIP が自己完結し、MSI 別取得の一手間が消える
- **自己完結の効能の射程**: ZIP 単体で完結する利点が実質的に効くのは**手動・少数端末導入**の
  シナリオである。Intune / SCCM 等の企業配布基盤では最終的に `.intunewin` 等へ再パッケージ
  するため MSI 同梱の有無は自己完結体験にほぼ寄与しない(この点でオプトイン同梱の価値は
  限定的であり、既定を非同梱に据える理由の一つでもある)
- 悪くなること(受け入れるトレードオフ): 選択肢 B の欠点 1〜4(候補検出・ZIP 生成の
  実装テスト、ビルド工程追加、版追従の変更半径拡大)。「検証済み Fluent Bit 版」の表明を
  Yagura 側が保守する責任を持つ——版の検証を伴わない案内更新はしない(conventions.md の
  実体検証原則)。MSI 同梱を選ぶと、検証済み版更新のたびに同梱運用側は MSI 再配置 → 再生成 →
  再配布が必要になる(非同梱なら MSI 更新は生成 ZIP に影響しない。この簡便性の喪失は
  同梱を選ぶ側のトレードオフとして明記する)
- リスク:
  - Fluent Bit 側の設定仕様変更(5.x 系等)への追従が遅れると、生成キットが最新 MSI と
    組み合わせられない期間が生じうる。「検証済み版」の明記で緩和する
  - Fluent Bit の脆弱性(CVE)公表を検知する体制を持たない場合、脆弱な版を「検証済み」と
    表明し続けるリスクがある(検知体制の整理は委任事項)。MSI 同梱時はこのリスクが
    配布物に焼き込まれるため、免責の明記と来歴記録で管理者の判断を支援するに留める
  - ダウンロード済み ZIP はその時点のサーバ設定の焼き込みであり、サーバの IP 変更後も
    古い宛先のまま使い回されうる。失効機構は設けず(複雑さがスコープに見合わない)、
    README の再生成注意と `GENERATED.txt` の来歴で対処する
- 本 ADR は転送の**観測性を改善しない**: 配布した端末からの転送が止まったことの検知
  (端末別の最終受信確認等)は範囲外であり、ADR-0001 決定 4(損失の観測可能性)との
  接続は委任事項として別途扱う

## 先送りにする場合の再評価トリガ

本 ADR が却下(選択肢 A 維持)となった場合、以下のいずれかで再評価する:

- 静的キットの試用フィードバックで宛先設定に起因する導入失敗が報告されたとき
- ネイティブ・エージェントレス収集(リモートイベントログ pull)の ADR を起案するとき
  (フォワーダ戦略全体を再整理する機会となるため)
- Fluent Bit に重大な脆弱性が公表され、「検証済み版」の更新体制の必要性が顕在化したとき

## 委任事項の一覧(追跡用)

| # | 委任事項 | 委任先 | 内容 |
|---|---|---|---|
| 1 | テンプレート埋め込みの具体機構 | 全体設計書(architecture.md)+ 実装 PR | MSBuild EmbeddedResource か前処理コピーか。`forwarder/fluent-bit/` を単一ソースとする成立条件 |
| 2 | `install.ps1` 置換ロジックの扱い | 実装 PR → 全体設計書 | サーバ側(C#)で置換した conf を同梱するため、`install.ps1` の置換パスとの関係(再利用 / 素通し)を確定して記録する |
| 3 | 「検証済み Fluent Bit 版」表明の置き場と更新手順 | 全体設計書 + ガイド | 生成 UI・生成 README・利用者ガイドの間で表明を一元化する。**表明文字列と実体(テンプレートの検証済み版)の乖離は CI で機械検知する**(更新忘れを人手運用に依存させない)。CVE 検知体制の整理(ADR-0005 SECURITY.md 管轄との境界)を含む |
| 4 | 転送の観測性(端末別の最終受信・途絶検知) | 別 ADR(v-next) | ADR-0001 決定 4 との接続。本 ADR の採否に関わらず必要性は残る |
| 5 | 生成操作の監査イベント採番・記録項目の詳細 | security.md + 実装 PR | ADR-0004 決定 7 の枠内での具体化。MSI 同梱時は「同梱有無・MSI の SHA256・ZIP の SHA256・版不一致警告の有無・警告承認の事実」を構造化して残す |
| 6 | 宛先候補検出(NIC 除外ロジック)のテスト境界 | 実装 PR | 除外条件(ループバック・リンクローカル/APIPA・無効化 NIC)と複数 NIC 混在環境の判定を単体テストで固定する(「除外する」という意図だけでなく、除外の実装が壊れていないことの保証) |
| 7 | MSI オプトイン同梱の実装細目(設計条件 9) | 実装 PR + security.md | 配置フォルダ ACL の実装(Administrators 限定の担保)・複数 MSI 時のエラー停止の実装・ProductVersion による版判定・公式 SHA256 の静的保持と自動照合の実現範囲・版不一致の二段階確認 UI・ZIP 全体 SHA256 の算出を確定して固定する |
| 8 | 導入手数の効果測定 | 試用フィードバック(v0.2 期) | 案 A(3 ステップ導線)+ MSI オプトイン同梱の組み合わせで、実際に導入完了までの手数・所要時間が短縮したかを試用で再確認し、次回 ADR 再評価 / v1.0 判定の材料にする |
| 9 | 検証済み版更新時の既配布 ZIP への影響ガイド | 利用者ガイド | 「自動通知はしない。来歴(`GENERATED.txt`)の保存を運用者側で徹底し、版更新時は同梱運用の再生成・再配布が必要」を明記して期待値のズレを防ぐ |

## 改訂履歴

一度書いた決定は変えない。以下は決定不変の補足・条件追加(amendment。status は accepted の
まま)。

1. **2026-07-07 (amendment): MSI のオプトイン同梱を追加(設計条件 9)。**
   - **契機**: 静的キットの実装・実機検証(PR #99・#101)と本 ADR の実装(PR #106)を経た
     試用フィードバックで、「生成 ZIP が Fluent Bit の MSI を含まず自己完結しない」点が
     直感的でないと指摘された。**この指摘の母数はオーナー 1 名であり、独立利用者からの反復
     報告ではない**(選択肢 A の補足と同じ透明性の作法で明記する)。案 A(生成画面の 3 ステップ
     導線。PR #106 で実装済み)の効果測定を経る前の判断であることも併記する。それでも進める
     のは、v0.2 スコープとしてオーナーが採用決定したため(効果は委任事項 8 で事後測定する)。
   - **変更**: MSI を**既定では非同梱のまま**とし、管理者が所定フォルダに手動配置した MSI に
     限りオプトインで同梱可能にした(設計条件 9)。中核決定(選択肢 B の採用 = 管理 UI での
     動的生成)・その他の設計条件は不変。
   - **なぜ supersession でなく amendment か**: 本 ADR の運用ルール(docs/adr/README.md)は
     「決定が変わったら supersession、補足・条件追加なら amendment」と定める。ここでの中核決定
     は「動的生成の採用」であり不変。加えて **MSI 同梱の既定は「非同梱」のまま変えていない**
     ——供給網統治を体験目標より優先する判断そのものは既定として保持され、オプトインは管理者が
     明示操作した場合の例外を追加するに過ぎない。もし**既定を同梱へ反転**するなら判断の逆転で
     あり supersession 相当だが、本改訂は既定を反転していないため amendment とする。
   - **元の非同梱理由 ①②③ の再評価(是正を含む)**:
     - ①(生成 ZIP が 20 MB 超になる): オプトインで管理者が選ぶため受容可。想定サイズは生成前に
       画面で予告する(設計条件 9)。
     - ②(Yagura リリースと Fluent Bit の版更新が結合する): **この理由は同梱の有無と直交して
       いた**——生成 UI が「検証済み版」を表明・保守する責任(委任事項 3)は MSI 同梱の有無に
       関わらず存在する。②を「非同梱の理由」に挙げていた元 ADR の切り分けが不正確だったと認め、
       ②はオプトイン同梱で悪化も改善もしない中立事項として扱う。
     - ③(改ざん・脆弱性対応の責任境界が曖昧になる): **「当たらない」ではなく「軽減されるが
       残る」**。管理者が取得・検証・配置した MSI であっても、Yagura の生成機能がそれを ZIP に
       封入して配布物を作る事実は消えず、Yagura は供給網上の信頼の連鎖に一段組み込まれる。
       Yagura は MSI の真正性を保証せず、来歴(SHA256)の記録と版不一致警告で管理者の判断を
       支援するに留める。この位置づけを免責として README・`GENERATED.txt` に明記する
       (設計条件 9)。「Yagura のリリース物・配布経路に第三者バイナリを含めない」という核
       (= リリース ZIP に MSI を焼き込まない)は維持されるが、それは責任境界の完全な消去とは
       別問題である。
   - **効果測定と再評価**(クリス round-2 (c) の指摘): 委任事項 8 の効果測定で、案 A + オプトイン
     同梱の組み合わせが導入手数・所要時間を実際に短縮したと確認できない場合は、本 amendment
     (オプトイン同梱)の縮小・撤回を含めて再評価する。「進める」の判断は測定結果でフィード
     バックされうる暫定判断である。
   - **申し送り(クリス round-2 (d))**: 設計条件 9 は具体パス・ファイル名パターン・命名規則・
     UI 仕様まで踏み込んでおり、ADR-0004 の記述規律(方針は ADR、実装詳細は全体設計書)に
     照らすと厚い。矛盾はないが、委任事項 7 の実装完了時に、実装細部を security.md 側へ一本化し
     本文を方針レベルへ圧縮する整理を行う。

2. **2026-07-10 (amendment): MSI 検出パターンを ARM64(`winarm64`)にも拡張(設計条件 9 の適用範囲拡大)。**
   - **契機**: [ADR-0009](0009-architecture-support.md) 決定7・委任 #4——Yagura サーバ本体の
     ARM64 対応拡張(Phase 1・PR #212 で実装済み)とは独立に、フォワーダキットの**収集対象端末**
     側にも ARM64 対応を拡張する。Fluent Bit は Windows 向けに `win64`(x64)・`winarm64`
     (ARM64)・`win32`(x86)の MSI を公式提供しているが(2026-07-10 ライブ確認)、Yagura が
     対応するのはサーバ本体と同じ判断基準で `win64`・`winarm64` の 2 アーキのみとし、`win32`
     は対象外とする(ADR-0009 決定1 の x86 不採用理由——需要の一次情報が無いこと・OOM リスク
     ——をそのまま踏襲。収集対象端末という文脈が変わっても再評価の理由にはならない)。
   - **変更**:
     - MSI 検出パターン(`ForwarderMsiConstraints.FileNamePattern` = `fluent-bit-*-win64.msi`)
       に加え、`FileNamePatternArm64` = `fluent-bit-*-winarm64.msi` を追加。判定は
       `ForwarderMsiFilter.IsCandidateFileName(string, ForwarderMsiArchitecture)` で
       アーキごとに独立させた
     - `IForwarderMsiSource.Lookup()` を `Lookup(ForwarderMsiArchitecture)` に変更
       (破壊的変更。実装は `SystemForwarderMsiSource` のみで、生成 UI 経由の呼び出し元も
       同一 PR で追随させた)
     - 管理 UI(`/admin/forwarder-kit`)に「収集対象端末のアーキテクチャ」の選択(x64 / ARM64。
       既定 x64)を追加。ダウンロードエンドポイントは `architecture` クエリパラメータ
       (既定 `x64`)を受け取り、選択アーキに対応する `Lookup` を呼ぶ
     - `install.ps1` に `Get-LocalMsiFilenamePattern` を追加し、実行端末のプロセッサアーキを
       判定して該当パターンの MSI のみを自動検出するようにした。判定の第一情報源は WMI/CIM
       (`Win32_Processor.Architecture`——ネイティブの WMI サービスが応答するため、呼び出し
       プロセス自身のエミュレーション状態に左右されない)とし、環境変数
       (`PROCESSOR_ARCHITECTURE` / `PROCESSOR_ARCHITEW6432`)は CIM 不可時のフォールバックに
       留める。環境変数のみの判定には「ARM64 端末上の x64 エミュレーションでは両変数とも
       `AMD64` を返し実 OS を反映しない」既知の限界があり、ARM64 端末を x64 と誤判定しうる
       ため(PR #222 レビュー指摘を受けて CIM 優先へ確定)。x86(32bit)上で実行した場合は
       明確なエラーで停止する
     - 公式配布 SHA256 の内部保持値を ARM64 分(`OfficialSha256ForVerifiedVersionArm64`)追加
       (2026-07-10 ライブ検証で確定。検証手順は既存の win64 分と同一)
   - **複数アーキ MSI 混在時の誤配布への牽制(委任 #4 の検討事項への回答)**: 追加の警告表示は
     実装しない。理由は、`Lookup(ForwarderMsiArchitecture)` が呼び出し時点で指定したアーキの
     MSI のみを候補とする設計そのものが、選ばなかったアーキの MSI を検出対象にすら含めない
     ——「警告して気づかせる」より強い「そもそも見えない」という構造的な牽制になっているため。
     同一フォルダに win64・winarm64 の MSI が 1 つずつ存在する状態(x64/ARM64 混在フレットへの
     配布に必要な正常な状態)は「複数検出」エラーにならない——アーキごとに独立して判定するため。
     画面側もアーキを切り替えると同梱チェックボックスの選択状態をリセットする(別アーキの
     MSI を誤って同梱要求してしまう事故の追加防止)。
   - **検証水準**: Fluent Bit の `winarm64` MSI が公式に取得できること・SHA256 が算出できること
     は 2026-07-10 に実機(x64 環境)で live 確認したが、**ARM64 実機での Fluent Bit 導入・
     サービス起動・イベント転送は本改訂の時点では未検証**(検証環境が x64 のため ARM64
     バイナリを実行できない)。アーキ自動判定も同様——CIM 経路・環境変数フォールバック経路の
     分岐ロジックはユニット相当の検証(実 CIM での x64 判定 + モックでの ARM64/x86 分岐)で
     確認済みだが、「ARM64 実機上の各種 PowerShell ホストで CIM・環境変数が実際に返す値」の
     実測は未実施であり、残余の限界(CIM 失敗時の環境変数フォールバックにおける x64
     エミュレーション誤判定の可能性)は利用者ガイドに回避手順(ARM64 ネイティブ PowerShell
     での実行または `-MsiPath` 明示指定)とともに明記した。ADR-0009 決定2 が Yagura サーバ
     本体の ARM64 を「試験的」と位置づけたのと同じ考え方を収集対象端末側にも適用し、
     実機検証は試用フィードバック・lab 実施の申し送りとする。詳細は
     [利用者ガイド §ARM64(winarm64)の検証状況](../guides/forward-windows-eventlog.md)を参照
   - **なぜ supersession でなく amendment か**: 中核決定(選択肢 B の採用・設計条件 9 の
     MSI オプトイン同梱の枠組み)は不変。本改訂は設計条件 9 が前提とする「対応アーキ」の
     集合を x64 のみから x64・ARM64 の 2 つへ広げる適用範囲の拡大であり、既存の判定ロジック・
     UI フロー・監査記録の構造(設計条件 9 全体)はそのまま維持している。
