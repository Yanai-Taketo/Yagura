# ADR-0017: 能動通知のメール送信チャネル（SMTP。opt-in）

- 状態: accepted（2026-07-18。PR #337 マージ）
- 日付: 2026-07-18
- 決定者: YANAI Taketo
- 関連: [architecture.md](../design/architecture.md) §4.6（能動通知）/ [ADR-0001](0001-project-founding.md)（スコープ制御）/ [ADR-0004](0004-security-model.md) 決定 5（資格情報の保管）・決定 7（監査）/ [security.md](../design/security.md) §1.1（外向き通信台帳）・§4.3（イベント ID 採番）/ [configuration.md](../design/configuration.md) §1・§2・§3・§8 / Issue #273 / PR #337

## 文脈と課題

architecture.md §4.6 の能動通知は「UI を見ていない夜間でも対応が必要な事象に気づける」ことを存在意義とするが、書き出し先は **Windows イベントログのみ**である。イベントログ転送・監視基盤（SCOM・Zabbix・イベントログ転送 + 集約等）を持つ環境ではこれで実効性があるが、本プロジェクトの主対象である中小規模の Windows-shop（ADR-0001。ひとり情シス〜小規模チーム）では、イベントログを常時監視する仕組みを持たないことが多いと想定される。その現場では現状の能動通知は実質「誰も気づかない」に近い（Issue #273。2026-07-16 の棚卸しで空白と認定）。

採用自体は 2026-07-16 のオーナー裁定で確定済み: SMTP によるメール通知は相関分析・インシデント管理のような商用 SIEM 領域（ADR-0001 の明示 NG）ではなく、**syslog サーバの基本機能の範囲**である（Kiwi Syslog Server 等の商用の同種製品もメール通知を基本機能として持つ）。本 ADR はその実現方式——opt-in の形・SMTP-AUTH / STARTTLS の扱い・認証情報の保管・送信失敗時の縮退・通知トリガ集合——を決める。

なお本機能は、逆引き DNS（ADR-0007）に続く**製品として 2 例目の自発的外向き通信であり、利用者が宛先を設定する型としては初**である。この性質（外向き通信台帳・監査・信頼ネットワーク境界の外への情報流出）への統制を決定に含める（ペルソナレビュー 田中の指摘）。

### スコープ（非目標）

商用 SIEM / アラート管理領域に踏み込まない線引きを最初に固定する:

- **ログ内容に基づくアラートルールは作らない**（「特定の文字列を含む syslog が来たらメール」等）。通知対象はあくまで architecture.md §4.6 の能動通知（= Yagura 自身の運用状態の異常）である
- **エスカレーション・オンコール輪番・確認応答（acknowledge）は作らない**
- **メール以外のチャネル（Slack / Teams / 汎用 webhook）は本 ADR では作らない**（再評価トリガは末尾）
- **トリガ単位の宛先振り分け・通知ルール編集 UI は作らない**（送るか送らないかの一括 opt-in のみ）

## 検討した選択肢

### 1. チャネルの構造 — 独自トリガ体系 vs 既存能動通知の第 2 の書き出し先

- **(a) メール専用の独自トリガ体系を新設する**: メール用に「何を通知するか」の条件・閾値を別に定義する。柔軟だが、トリガ定義が二重管理になり、閾値・抑制窓のチューニング面も倍になる。「通知ルールの編集」という SIEM 的 UI へ滑り出す入口にもなる。**却下**
- **(b) 既存の能動通知事象をそのまま第 2 の書き出し先（メール）へも送る**: トリガの定義・評価・抑制窓は既存の能動通知機構（`ActiveNotificationMonitor` の周期監視 + 発生箇所からの即時通知）を唯一の正とし、メールはイベントログと並ぶ**書き出し先の追加**に徹する。定義の二重化がなく、イベントログとメールで「見える事象」が食い違わない。**採用**
- **(c) 作らない（イベントログ転送・OS のタスクスケジューラのイベントトリガ起動に委ねる）**: 追加コード 0。ただし対象現場（監視基盤なし）が自前で組む場合、①イベントトリガのタスクを対象イベント ID ごとに設定する必要があり（十数件）、②抑制・流量制御がなく、③送信スクリプトに SMTP 資格情報が平文で置かれがちで、④送信の標準手段だった `Send-MailMessage` は Microsoft 自身が非推奨化済み（本 ADR が `SmtpClient` を却下したのと同じ系統の理由）——「各自組む」の実質難度は低くない。オーナー裁定（2026-07-16）で採用が確定しており、却下の経緯として記録する。なお**監視基盤を持つ現場では、メールを有効化せずイベントログ転送のレールに乗せることが引き続き第一選択**である（利用者向けドキュメントに明記する——申し送り D-4）

### 2. 送信の実装 — 依存ライブラリ

- **(a) `System.Net.Mail.SmtpClient`（BCL 標準）**: 追加依存 0。ただし公式ドキュメントが新規開発での使用を明示的に非推奨とする——「We don't recommend that you use the `SmtpClient` class for new development because `SmtpClient` doesn't support many modern protocols. Use MailKit or other libraries instead.」（learn.microsoft.com の SmtpClient クラス Remarks。2026-07-18 確認）。廃止予告付きの API に新機能を載せるのは v1.0 を目指す本体には不適。**却下**
- **(b) MailKit**: .NET のメール送信のデファクトであり、上記の公式ドキュメントが代替として名指しする。SMTP-AUTH（PLAIN / LOGIN / CRAM-MD5 / OAuth 等）・STARTTLS を網羅する。**採用**。依存追加の作法として、**実装 PR の時点で当日の最新版をライブ検証してから採用版を確定する**（CLAUDE.md の執筆スタイル規約。本 ADR ではバージョンを固定しない）。推移的依存（MimeKit・暗号系）を含む依存ツリー全体を実装 PR に記録し、`Directory.Packages.props` の集中管理 + Dependabot + dependency-review の既存供給網統治に乗せる

### 3. 暗号化（STARTTLS）の既定

対象現場の SMTP サーバは「社内 Exchange / 内部リレーの 25 番・無認証・STARTTLS 任意」から「submission 587・AUTH 必須・STARTTLS 必須」まで幅がある。

- **(a) STARTTLS 必須固定**: 最も安全だが、STARTTLS を提供しない内部リレーで機能自体が使えない。信頼ネットワーク前提 + opt-in 強化（ADR-0004）の既定思想とも整合しない。**却下**
- **(b) 3 値の明示設定**: `none`（使わない）/ `auto`（サーバが対応すれば使う = opportunistic。**既定**）/ `required`（必須。確立できなければ送信失敗として扱う）。既定 `auto` は「内部リレーでもそのまま動き、対応サーバなら自動で暗号化される」——平文受信が既定の syslog 本体と同じ「既定は動くこと・強化は opt-in」の整理。**採用**。ただし資格情報を設定した構成での `auto` は STARTTLS ストリップにより**資格情報自体**が平文で流れ得るため、能動的な警告を課す（決定 3）
- implicit TLS（SMTPS 465）は初版では対象外とする（`required` + 587 で実用は満たせる。要望が出た時点で `Security` の第 4 値として追加できる）

### 4. SMTP-AUTH の必須性

- **(a) AUTH 必須**: 無認証の内部リレー（Exchange の受信コネクタ等、Windows 環境で広く使われる想定）で使えない。**却下**
- **(b) 任意**（ユーザー名・パスワードが設定されていれば AUTH、未設定なら匿名）: 内部リレー・submission の両構成に 1 つのキー体系で対応できる。**採用**

### 5. 送信失敗時の扱い

- **(a) 永続キュー + 無制限再試行**: 「必ず届ける」に寄せる案。メールを記録の正本に近づけてしまい、ディスク管理・順序・重複の複雑さを持ち込む。メールは「気づくきっかけ」であり、正本はイベントログと監査記録である。**却下**
- **(b) 有界メモリキュー + 有界再試行 + 破棄（at-most-once）**: 送信を受信・保存経路から完全に独立させ、失敗の限界を明示する。**採用**（詳細は決定 5。破棄しても事象自体はイベントログに残る）

### 6. 通知トリガ集合

- **(a) トリガ選別 UI**: 「どれを送るか」を選ばせる。通知ルール編集（非目標）の入口になる。syslog 導入初心者には選別の判断材料もない。**却下**
- **(b) 対象イベント ID の明示リスト（allowlist）で「全部」を定義する**: 選別 UI は作らず、製品として対象を固定する。**採用**。「全部」を「1000 番台」「警告以上」のような性質で定義すると、能動通知でない運用警告（1020 再起動待ち残存・1021 再読み込み拒否等）や**メール送信失敗の新 ID 自身**が混入し、ループ排除が言葉の解釈に依存してしまう（ペルソナレビュー クリス・リサの指摘）——membership は明示リストで固定し、メール送信失敗 ID をリストに含めないことで**ループを定義レベルで排除**する（決定 6）

## 決定

### 1. 能動通知の第 2 の書き出し先としてメール送信を追加する（opt-in・既定無効）

- 新設キー `Notification:Email:*`（既定 `Enabled=false`）。ゼロ設定ファーストランには一切現れない
- トリガの定義・評価は既存の能動通知機構を唯一の正とし、メールは書き出し先の追加に徹する（選択肢 1-(b)）
- 通知メールの本文は、イベントログに書くのと同じ情報（事象の説明・発生時刻・イベント ID・本文メッセージ）の**日本語プレーンテキスト**とする（イベントログ本文の日本語化と同じ整理。多言語化は意図的先送りに従う）。HTML テンプレート・カスタマイズは作らない。本文に載せるフィールドの上限列挙は実装 PR で固定し、以後 additive にのみ増やす（受信 syslog の本文・資格情報・接続文字列は構造的に載らない——載せる情報源をイベントログ書き出しと同一にすることで担保する）

### 2. 接続と暗号化

- 新設キーは `Notification:Email:{Enabled, From, To}` + `Notification:Email:Smtp:{Host, Port, Security, Username, Password}` の 8 キー（SMTP 接続属性は `Smtp` 階層へまとめ、既存体系の階層切りに合わせる）
- `Port` 既定 25、`Security` = `none | auto | required`（既定 `auto`）
- `required`: サーバ証明書の検証は**既定有効**。STARTTLS を確立できない・証明書検証に失敗した場合は**送信失敗として扱う**（平文へ降格しない——fail-closed）。社内 CA・自己署名証明書への緩和を設ける場合は独立した opt-in キー（明示設定 + 警告付き）とし、方式は実装設計で確定する（委任 3）
- `auto`: STARTTLS を試行し、確立できない場合（サーバ非対応・証明書検証失敗を含む）は**平文で送信を継続する**（opportunistic の定義どおり。この降格リスクは帰結と利用者向けドキュメントに明示する）
- **不正時挙動（configuration.md §1 の 3 分類の宣言）**: `Notification:Email:*` 配下のキーの不正値（`Security` の未知値・`Port` の範囲外・`From`/`To` の形式不正）、および `Enabled=true` で必須項目（`Smtp:Host`・`From`・`To`）が欠落した構成は、**メール通知機能を無効化して警告する**（縮小側で継続。受信・保存には一切影響しない。緩い側の `auto` へも黙って倒さず、送信が黙って死ぬ `required` へも倒さない——「設定したのに送られていない」を警告で必ず見せる）

### 3. SMTP-AUTH と資格情報

- `Smtp:Username` / `Smtp:Password`（任意。両方設定されたときのみ AUTH）。**片方のみ設定された構成は不正構成として決定 2 の規則（機能無効化 + 警告）で扱う**——黙って匿名送信へ落とさない（「設定したのに使われていない」を作らない）
- `Password` は ADR-0004 決定 5 の様式を踏襲する: UI 経由の入力は常に DPAPI で暗号化して `dpapi:<Base64>` 形式で保存し、手編集の平文は受理しつつ警告する。DPAPI entropy は SQL Server 用と分離した専用名前空間 **`Yagura:Notification:Email:Smtp:Password:v1`** とする（版管理付き。`DpapiConnectionStringProtector` の機構を一般化して再利用）
- **復号失敗時（改ざん・破損・別マシンへの構成コピー）はメール送信を停止して警告する**（fail-notify）。無認証送信への黙示フォールバックはしない（誤送信・送信元アカウントのロック誘発を避ける）
- **`Password` 設定済み + `Security` ≠ `required` の構成は、設定保存時・起動時・再読み込み時に警告する**（configuration.md §1 の「能動的に見せる警告」の系。STARTTLS ストリップで漏れるのは通知内容ではなく SMTP 資格情報——多くの環境では AD アカウント——である旨を警告文言に含める）
- パスワード値はログ・監査・UI 再表示のいずれにも出さない。送信失敗イベントに SMTP サーバ応答を含める場合、AUTH 交換部分は記録しない（実装要件）

### 4. 宛先・差出人と監査

- `From` / `To`（`To` は JSON 配列。既存の配列キー——`Admin:Authentication:Windows:AdminGroups` 等——と**同じ JSON 表現**。ただし既存の配列キーは手編集のみで、**配列キーを UI から保存する経路は本 ADR が初めて要求する**——実装は委任 9）。宛先ごとの振り分けはしない
- **メール通知設定の変更は専用の監査 ID（2000 番台新設）で、変更キーと新値を含めて記録する**（`Password` は変更の事実のみ）。宛先・接続先は「通知がどこへ向かうか」= 流出経路そのものの定義であり、キー名粒度（2016 の「前後値を含めない」）では事後に追えない——値を残す前例は ADR-0012 決定 7（2012 = 証明書拇印・ポートの新値を記録）に既にある。手編集 + 再読み込み経路は従来どおり 2016（キー名粒度）に合流し、手編集 + 再起動は起動時差分照合 2019（Issue #329）が変更キー名を残す（この経路の限界は security.md §4.1 の受容のまま）
- **外向き通信台帳（security.md §1.1）を拡張し、SMTP 送信を登録する**（宛先 = 利用者設定の SMTP サーバ・契機 = 能動通知/テスト送信・無効化手段 = `Enabled=false`）。台帳の現行スコープ（閲覧層）を製品全体へ広げるか Host 側の相当欄を新設するかは実装 PR で確定する（委任 4）

### 5. 送信の独立性と縮退

- **有界メモリキュー（上限 仮値 64 件）+ 背景送信**。受信・保存・UI への影響経路を作らない。キュー溢れ時は**古い側を破棄して新しい側を保全**する（破棄数はライブ計器に計上）。監査の SEC-10（障害起点の保全 = 古い側保全）とは**意図的に逆**——監査は証跡だから発生順が正だが、メールは「今の異常に気づくきっかけ」であり、SMTP 長時間停止からの復旧時に届くべきは最新の状態である（ペルソナレビュー 鈴木の指摘）
- **SMTP タイムアウト**: 接続 10 秒・送信 30 秒（仮値）。**再試行は 1 通あたり 1 回、間隔を空けて行う（仮値 5 分後）**——即時再試行は落ちているサーバ・greylisting（初回配送の意図的一時拒否）に対して無力である。それでも失敗した通知は破棄する（at-most-once。正本はイベントログと監査記録）。**再試行待ちの通知はキュー枠を占有したまま後続の送信を塞がない**（待ち時刻付きで保持し、待ちのない通知を先に送る——head-of-line blocking を作らない）
- **複数宛先の一部 RCPT 拒否は「メッセージとしては送信成功・拒否宛先を警告ログ」とし、再送しない**（成功済み宛先への二重送信を作らない。実装での固定とテストは委任 7）
- 送信失敗はイベントログの警告（新 1000 番台 ID・抑制窓付き）+ ライブ計器で観測可能にする。**メール送信の失敗はメールで通知しない**——ループは決定 6 の allowlist にメール失敗 ID を含めないことで定義レベルで排除する
- **チャネル健全性の常設表示**: 認証済み管理面のダッシュボードに、最終送信成功時刻・直近の失敗理由・キュー深度・累積破棄数・**流量上限による抑制の状況・設定不正による機能無効化の状態（決定 2）**の常設カードを置く。**抑制の表示は累積回数だけにしない**——回数のみでは「先週の 1 回の障害でついた数字」と「毎晩じわじわ増えている」が区別できず、また**何が届かなかったかが特定できない**（改訂 1 のペルソナレビューでの指摘）。**累積回数 + 直近に抑制が発生した時刻 + 抑制された EventId の内訳**を出し、閾値を設定キー化しない判断（再評価トリガ）の材料と、事後にイベントログのどこを見ればよいかの手掛かりの両方を satisfy する。カウンタの永続性（プロセス内累積か再起動を跨ぐか）は実装 PR で確定する（#286/#318 の再起動待ちキー常設表示と同じパターン）。無効化警告の一次到達先がイベントログであることと「イベントログを見ない現場」の前提の循環は、このカードが閉じる。「イベントログを見ない現場」という本 ADR 自身の前提の下で、チャネルの静かな死（パスワード失効・リレー廃止等）に日常動線で気づける経路を確保する——at-most-once + 破棄の正当化はこの常設表示があって成立する（ペルソナレビュー 佐藤・鈴木・クリスの指摘）
- `Enabled=false` への即時反映時、キュー内の未送信通知は破棄する（送り切りを待たない——無効化の意図を優先）

### 6. トリガ集合と流量制御

- **対象はイベント ID の明示リスト（allowlist）で定義する**。初版の対象は実装済みの能動通知事象: スプール系（1001〜1007）・自己検証失敗（1009・1010）・周期評価失敗（1008）・証明書期限接近/使用不能（1014・1015・1017・1018）・認証防御の昇格（1019）・**受信リスナの縮小継続（1022）**を基本とし、確定は実装 PR で行う。**メール送信失敗の新 ID はリストに含めない**（ループの構造的排除）。リストは security.md §4.3 の ID 表に「メール通知対象」マーカーとして記録し、**allowlist と実際の配線の一致を検証するテストを実装 PR に含める**（将来 ID を追加する PR が「メールに乗るか」を明示的に判断することを表の更新義務で強制する——既定は対象外）
- **同一トリガのメール再送間隔はイベントログの抑制窓より長くとる（仮値 60 分）**。イベントログに 1 件ならメールも最大 1 通、の上限関係は維持したまま、持続条件（スプール上限接近等）が一晩で数十通を積む事態を防ぐ
- **メール層の同一性判定は EventId 粒度とする**（イベントログ側の抑制キー——送信元 IP 別・ボリューム別のトリガキー文字列——より意図的に粗い。ILoggerProvider に渡る情報で判定できる粒度であり、発火点のコードに触らないという決定 7 の採用理由を守る）。帰結として、**同一 EventId 内の別インスタンス（1019 の別攻撃者・1006 の別ボリューム等）は再送間隔内は 1 通に畳まれる**——上限関係（メール ≤ イベントログ）は保たれ、詳細の全量はイベントログが持つため、「気づくきっかけ」の役割には足りると整理して受容する（インスタンス粒度が必要になった場合の手段——構造化ログ state でのトリガキー搬送——は再評価トリガへ）
- **全体流量上限（仮値 10 通/時）**を置く。超過時の選好: ①エラーレベルの事象（1008・1009 等）は上限の対象外とする ②警告レベルでは**未送信トリガ種の初報を、送信済みトリガ種の再送より優先する**——同一障害の重複通知が枠を食い尽くして希少で重要な初報（認証防御の昇格等）を抑制する優先度逆転を防ぐ（固定 2 段。設定・編集 UI は作らない）。抑制の発生はイベントログへ 1 回だけ警告し、常設カード（決定 5）にも表示する
  - **①「エラーレベルは上限の対象外」は無制限を意味しない**（改訂 1 で導出を明記）: EventId 粒度の再送間隔（仮値 60 分）が上位で効くため、実効上限は概ね **「allowlist 中のエラー ID 数 × 60 分あたり 1 通」** に収束する。この導出をもって、上限対象外が深夜の大量送信経路にならないことを確認済みとする
  - **同じ理由で、全体流量上限が実際に発動するのは大規模障害時に限られる**——同一 EventId が 60 分に 1 通である以上、送信量は allowlist の要素数に自然に律速される。上限がバインドするのは「多数の異なる事象が 1 時間以内に同時発火した」場面であり、**通知が最も必要な場面と重なる**。したがって超過時の選好が正しく働くことは単体テストで固定する（受け入れ基準に既記載）
  - **allowlist の各 EventId の重大度（= 流量上限の対象外か否か）は security.md §4.3 の表に列として持つ**（改訂 1 で追加）。優先度の挙動を「ID ごとのログレベル」という別の場所の暗黙値に依存させない
- 上記の仮値 6 点（キュー上限 64・再試行間隔 5 分・再送間隔 60 分・流量上限 10 通/時・SMTP 接続タイムアウト 10 秒・送信タイムアウト 30 秒）は configuration.md §9 の確定待ち一覧（CF-x）へ登録し、実測で確定する（設定キーとしては公開しない）

### 7. 実装の位置 — ILogger レール上の第 2 のシンク

能動通知の発火点は 1 箇所ではない: `ActiveNotificationMonitor`（Host。周期系）・`PersistenceWriter`（**Yagura.Ingestion**。即時系 1005）・起動時経路（Program.cs。1001 等）・`AdminAuthFailureDefense`（1019）。受信機構は 2 案を比較した:

- **(a) 発火点直呼びのフック**: 散在する全発火点に二重書き（ILogger + フック呼び出し）を足すことになり、フック契約を `Yagura.Ingestion` から参照できる層（`Yagura.Abstractions`）へ公開する必要が生じる。配線漏れが構造的に起こり得る（選択肢 1-(b) の「食い違わない」が実装規律頼みになる）。**却下**
- **(b) カスタム `ILoggerProvider` を第 2 のシンクとして Host の合成ルートで登録する**: 挿入点は 1 つ・発火点のコードは一切触らない。EventLog プロバイダと同じ `ILogger` レールに乗るため、**Ingestion 発の即時通知・起動時イベント（DI ロガー経由で Build 後に発火するもの）も自動的に捕捉できる**。対象の選別は決定 6 の EventId allowlist で行う。**採用**

- プロバイダの契約: **有界キューへの投入のみを行い、ブロックせず、例外を呼び出し元へ漏らさない**（ロギング呼び出しが受信ホットパス上にあるため）。送信の実体（キュー消化・SMTP 接続）は Host の背景コンポーネント（`Yagura.Host.Observability.ActiveNotification` 配下）が担う
- **利用者の `Logging:*` フィルタ設定から独立させる**: 本プロバイダはプロバイダ固有のコード側フィルタで全カテゴリ・全レベルを受け取り、選別は決定 6 の allowlist のみで行う。イベントログのノイズ調整のつもりの `Logging:LogLevel` 変更が**黙ってメールを止める**経路を作らない（決定 2 の「設定したのに送られていない状態を警告で必ず見せる」との一貫。独立性の検証は委任 6 のテストに含める）
- **起動時経路の allowlist ID（1001・1022 等）がプロバイダ登録後に発火することを実装 PR で確認する**。登録前に発火し捕捉できない ID が見つかった場合は、発火の遅延か allowlist からの除外（+ 帰結への記録）を個別に決める（委任 6）
- 将来のチャネル追加（Slack / webhook 等）は、同じレールに別のシンク（`ILoggerProvider` またはキューの別コンシューマ）を足す形で実現できる——`Yagura.Abstractions` への公開契約の新設は初版では不要（要望が出た時点で再評価）
- UI（設定画面・テスト送信・常設カード）は既存の管理画面パターン（`Yagura.Abstractions` 契約 + Host 実体 + `Yagura.Web` 画面）に従う

### 8. テスト送信

- 管理画面に**テスト送信**を置く。**画面に入力中の（未保存の）値で送信できる**——保存前の検証こそがテスト送信の価値であり、誤設定を保存しないと試せない作りにしない。結果（成功 / 失敗と理由）を画面に即時表示する。テスト送信は**キュー・流量上限を経由しない直送**とする（テストが実通知の枠を消費しない）。「直送」はキュー非経由の意であり、**UI 操作としては非同期実行 + 同時実行 1 件 + キャンセル可能**とする（タイムアウト上限——接続 10 秒 + 送信 30 秒——の間、管理画面の操作を塞がない。受け入れ基準「UI 応答が影響を受けない」との整合）
- **パスワード欄が空欄のままのテスト送信は、保存済みの `Smtp:Password`（DPAPI 復号値）を使用する**（パスワードは UI に再表示しないため、空欄 = 「変更しない」の意味に固定する）。この形態は「画面上の任意ホストへ保存済み資格情報で AUTH を試行できる」操作になるため、**監査レコードに「保存済み資格情報を使用」の別を含める**（値は含めない）
- **テスト送信は監査対象とする**（2000 番台の新 ID。接続先・宛先・成否・操作者を記録し、資格情報値は記録しない）。「状態を変えない操作」だが、①資格情報の使用 + 外向き実トラフィックを伴う点で本番昇格の接続検証（2002）と同型であり、②未保存値で任意ホスト・ポートへ接続を試せる操作は内部ネットワークの到達性探査に転用し得るため、「誰がいつどこへ試したか」を証跡に残す（ペルソナレビュー 田中の指摘。監査対象外とした当初案を撤回）

### 9. 反映方式

- `Notification:Email:*` は**即時反映**（CF-4 の `ImmediateConfigurationApplier` に登録。送信クライアントは接続を保持しないため参照交換で足りる）。`Password` の DPAPI 復号の再読み込み経路への配線・configuration.md §8 表への全キー記録を実装 PR に含める

## 受け入れ基準

- 設定画面から保存 → テスト送信成功 → 実際の能動通知（例: スプール上限接近を人工的に発生）がメールで届くエンドツーエンドが成立する
- SMTP サーバ停止・応答遅延・TCP ブラックホールのいずれでも、受信スループット・UI 応答・監視ループの周期評価が影響を受けない
- `required` で STARTTLS 不成立の場合に平文送信が発生しない（パケットレベルで確認）
- allowlist と配線の一致検証テスト・流量上限とエラー優先の単体テスト・`Password` 復号失敗時の fail-notify テストが存在する

## 帰結

- 良くなること: 監視基盤を持たない現場でも、夜間のスプール成長・証明書期限・認証攻撃の予兆に**人が届く経路**で気づける。能動通知の存在意義（§4.6）が対象現場で初めて実効化する
- **受理時の波及**: architecture.md §4.6 に「第 2 の書き出し先（メール。opt-in）」を追記し、「イベントログは既存監視のレールに乗る」という前提と「そのレールを持たない現場がある」という本 ADR の前提の関係（前者はレールを持つ現場の整理・後者への補完が本機能）を §4.6 側でも整合させる（実装 PR で実施——委任 5）
- 悪くなること・受け入れるリスク:
  - 外部依存（MailKit + 推移的依存）が増える（供給網統治は既存の枠組みに乗せる）
  - `Security=auto` の既定は STARTTLS ストリップ（中間者による平文降格）に対して無防備である。信頼ネットワーク前提（ADR-0004）の範囲内と整理し、敵対的経路を挟む構成では `required` を使うことを文書で案内する。資格情報を設定した構成の降格リスクは決定 3 の能動警告で補う
  - パスワードを設定ファイルに置く形式（DPAPI 暗号化）はサーバ束縛であり、構成配布運用と両立しない——既知の限界（configuration.md §2 CF-D3）をメールにも延長する
  - メールは at-most-once（有界再試行 + 破棄）。特に**起動時の一度しか発火しない事象（1001 縮退起動等）は、復電直後などメールリレー側が未収束のタイミングでは再試行 1 回の後に恒久喪失し得る**。「メールが来ない = 正常」とは限らない——正本はイベントログであり、チャネル健全性は常設カード（決定 5）で確認する
  - **通知メールの配送経路・宛先メールボックスは信頼ネットワーク前提の外にある**。宛先がクラウド（M365 等）の構成では、イベントログ同等の運用・セキュリティ情報（攻撃元 IP・狙われたアカウント名・内部ホスト名等）が管理セグメント外へ恒常的に出ていく。機微性の高い環境では宛先を管理内メールシステムに限定することを利用者向けドキュメントで案内する（申し送り D-3）
  - **Exchange Online（M365）の SMTP AUTH（Basic）は、2020 年 1 月以降作成のテナント・Security Defaults 有効環境では既に既定で使えない**（改訂 1 の調査）。本 ADR の主対象層はこの条件に該当しやすく、**社内リレーを持たず M365 直送しか経路がない現場では、初版のメール通知が利用できない構成が現に存在する**——「将来の可能性」ではない。この層への案内は**社内リレー / オンプレ Exchange 経由を第一選択**とする（申し送り D-6）。なお**テナント側で SMTP AUTH を再有効化する選択は Basic 認証の復活 = セキュリティ既定の後退**であり、組織のポリシー上そもそも許可されない場合がある。**Yagura として再有効化は推奨しない**（他社製品のセキュリティ既定を弱めさせる行為であり、ADR-0004 の「既定は緩く強化は opt-in」——自製品の話——とは別物）。恒久的な解は再評価トリガ（OAuth）参照
  - **MailKit / MimeKit は事実上単一のメンテナに支えられている**（作者本人が [MimeKit の SECURITY.md](https://github.com/jstedfast/MimeKit/blob/master/SECURITY.md) で「the MimeKit team is small (currently only myself)」と明言。2026-07-19 確認）。それでも採用するのは、(a) Microsoft の公式ドキュメントが代替として名指しする実質的な標準であること、(b) SMTP という枯れた仕様の実装であり、メンテナンスが停止しても機能縮退（送信機能の無効化）で受信・保存の可用性に波及しない独立経路として設計されていること——による。累計 DL は 2 億件超（[nuget.org/packages/MailKit](https://www.nuget.org/packages/MailKit) / [MimeKit](https://www.nuget.org/packages/MimeKit)。2026-07-19 確認）・MIT ライセンスでありフォークの法的・実務的障壁も低い。**メンテナンス停止の兆候（長期のリリース途絶・セキュリティ報告への未応答）を観測した時点で再評価する**

## 委任・未解決（実装 PR へ）

| # | 委任事項 | 内容 |
|---|---|---|
| 1 | MailKit の採用版確定 | 当日最新版のライブ検証 + 依存ツリー全体（直接 + 推移）の記録。`Directory.Packages.props` 集中管理への取り込み |
| 2 | 本文フィールドの上限列挙 | 通知メール本文に載せるフィールドを固定（以後 additive のみ）。SMTP サーバ応答の記録から AUTH 交換を除外する実装 |
| 3 | `required` の証明書検証緩和 | 社内 CA・自己署名対応の独立 opt-in キーの要否と方式（TLS 受信証明書 = security.md §6 の整理を参照） |
| 4 | 外向き通信台帳の拡張方式 | **✅ 確定（2026-07-20。Issue #368）: 台帳スコープを製品全体化**し（security.md §1.1——層の列を追加。Host 側相当欄の新設は「どこへ何が出ていくか」に 2 か所を見ないと答えられなくなるため不採用）、SMTP 送信・テスト送信を登録した。宛先欄に「利用者設定次第で管理セグメント外・公衆網上の外部サービスになり得る」を明記済み（改訂 1 の要求どおり。台帳は「どこへ何が出ていくか」に答える道具であり、社内リレーを暗黙の前提にした行は将来の監査で誤答を生む）。あわせて保存先 DB 接続を対象外とする線引きを台帳に明文化した |
| 5 | architecture.md §4.6・security.md §4.3・configuration.md §8 の改訂 | 第 2 の書き出し先の追記・新イベント ID 採番（送信失敗警告・設定変更監査・テスト送信監査）・メール通知対象マーカー列・全キーの表記録。**§4.3 表の 1019 の二重登録（表の行 = 設定読み取り失敗〔未実装・Issue #312 予約〕vs 末尾文章とコード実体 = 認証防御昇格）の解消を含める**——未実装側を 1024 以降へ再採番し configuration.md CF-D8 を追従させる。マーカー方式は表の一意性が前提であり、解消なしには成立しない |
| 6 | allowlist 配線検証テスト | 決定 6 のリストと実配線の一致を**ログ呼び出しレベルで**機械検証する（定数表との突合に留めない。**EventId 未指定の通知系ログの検出**——`[permanent-failure]` 等、EventId 欠落は #323 でも実際に起きた欠陥クラス——を含む）。**起動時経路 ID のプロバイダ登録後発火の確認**（決定 7）と、**利用者 `Logging:*` 設定からの独立性の検証**を含める |
| 7 | 部分拒否の実装固定 | 決定 5 の方針（一部 RCPT 拒否 = 送信成功・拒否宛先を警告ログ・再送しない）の実装固定とテスト |
| 8 | Exchange Online SMTP AUTH のライブ検証 | **文献調査は完了（2026-07-19。Issue #350 / PR #365）——結果と出典は改訂履歴 1。結論: 初版スコープ外を維持。** ただし実施内容は**公式ドキュメントの調査であり、実機で M365 へ SMTP 接続を試してはいない**（M365 テナントを要するため）。**実装 PR 時に実機確認を行う**——本調査は当時の事実の記録であり、実装時点の提供状況を代替しない。あわせて M365 直送構成での送信失敗の報告があった時点でも再確認する。**実機確認には、改訂履歴 1-A で「確認できなかった」とした 2 点——`SMTP.SendAsApp` の GA / プレビューの別、および Application Access Policy / RBAC for Applications による権限限定の可否——を含める**（後者は再評価トリガ (b)「送信専用に限定した権限が提供された」の判定に直接必要であり、未検証のまま放置すると「未検証 → 不発火 → 誰も検証しない」の循環になる）（**再確認の期限を日付で置かない**のは改訂 1 の方針の裏返し） |
| 9 | 配列キー（`To`）の UI 保存経路 | 既存の配列キーは手編集のみ（ウィザードの差分適用対象外）であり、**配列キーの UI 保存は本 ADR が初めて要求する新機構**。保存表現・検証・§8 表記との整合を実装で確定する |
| 10 | `[permanent-failure]`（EventId なし）の扱い | 恒久障害開始の通知ログに EventId を採番してメール対象に含めるか、1004（退避継続）経由の間接検知で足りると受容するかを実装 PR で判断し、結果を帰結へ記録する |
| 11 | **抑制・流量制御の適用点**（改訂 1 で追加） | 決定 6 の「同一 EventId は 60 分に 1 通」と「全体流量上限」を、**キュー投入時（enqueue）に評価するか送信時（dequeue）に評価するか**を固定する。**送信時とした場合、決定 5 の「キュー溢れ時は古い側を破棄」が決定 6 の「未送信トリガ種の初報を優先」と衝突する**——抑制対象も含めて全件がキューに積まれるため大規模障害時に 64 件がすぐ埋まり、古い側破棄によって**先に入っていた異種の初報が、後から来た同種の重複に押し出される**（決定 5 の「古い側破棄」は同一事象の連続を前提にした理屈であり、異種の初報が並ぶキューでは成立しない）。この場合は破棄選択にトリガ種の未送信性を反映させる必要がある。****衝突した場合は決定 6 の選好（未送信トリガ種の初報を優先）を上位とする**——これは実装の詳細ではなく方針の選択であり、決定 6 が優先度逆転の防止を明示的な選好として既に書いているため ADR で決める。**委任するのは適用点（enqueue / dequeue）の選択と、それに伴う破棄アルゴリズムの実装のみ**。整合を実装 PR で明示し、テストで固定する |
| 12 | **テスト送信・失敗表示の平易化**（改訂 1 で追加） | 代表的な SMTP 失敗（認証拒否・STARTTLS 不成立・接続不能・リレー拒否）について、**生のサーバ応答文字列だけを出さず**、平易な日本語の説明と次の一手を画面・常設カードに添える。とくに `SmtpClientAuthentication is disabled` 相当の応答は申し送り D-6 へ誘導する（**判定は応答文字列の部分一致に留め、日付やスケジュールには言及しない**）。決定 8 の「結果（成功 / 失敗と理由）を画面に即時表示する」の「理由」が SMTP の生文字列のままだと、原因に辿り着けない利用者が**パスワードを打ち直してアカウントをロックする**——決定 3 が明示的に避けたい事態そのものになる（改訂 1 のペルソナレビューでの指摘） |
| 13 | **認証方式の設定形**（改訂 1 で追加） | 決定 3 の認証設定を、**将来 oauth2 を additive で追加できる選択肢の形**にする（初版の値は「認証なし」「ユーザー名 + パスワード」の 2 つ）。Zabbix が Authentication = なし / ユーザー名+パスワード / OAuth の 3 択を持つのに対し、Grafana・Graylog・Kiwi は user と password が生えているだけで、後から認証方式を足すと設定体系ごと作り直しになる（改訂履歴 1-B 点 3）。**枠だけ先に切っておけば後から値を 1 つ足すだけで済む** |

## 利用者向けドキュメントへの申し送り

| # | 内容 |
|---|---|
| D-1 | 環境パターン別の設定例(最低 2 つ: 社内リレー〔25/auto/認証なし〕・submission〔587/required/認証あり〕)。専門用語(opportunistic 等)を使わない平易な対応表 |
| D-2 | `auto` の降格リスクと `required` の使い分け。`required` + 社内 CA 証明書環境の手順 |
| D-3 | 通知メールが管理セグメント外へ出る構成(クラウドメールボックス)の注意。機微環境では管理内メールシステムへの限定を推奨 |
| D-4 | 監視基盤(イベントログ転送等)を持つ現場ではメールを有効化せず既存レールを第一選択とする案内 |
| D-5 | 「メールが来ない = 正常とは限らない」——正本はイベントログ・チャネル健全性は管理面の常設カードで確認する運用 |
| D-6 | **M365（Exchange Online）を送信先にする場合の案内**（改訂 1 で追加）。①**自テナントで SMTP AUTH が使えるかの確認手順**（2020 年 1 月以降作成のテナントは既定オフ・Security Defaults 有効時は無効・条件付きアクセスが上位で遮断——**主対象層ほど該当しやすい**）。②使えない場合の代替経路を**優先順位つきで**示す: **① 社内リレー / オンプレ Exchange 経由（第一選択。Exchange Online の SMTP relay コネクタは IP または証明書ベースで資格情報を要求しないため、本 ADR の「認証なしリレー」モードがそのまま使える）→ ② 別の SMTP プロバイダ → ③ Azure Communication Services Email**。各経路に要る作業の重さを併記する。③**テナント側で SMTP AUTH を再有効化する選択は推奨しない**（セキュリティ既定の後退であり組織ポリシー上許可されない場合がある）。④ACS を選ぶ場合の注意——**送信ロールのみを持つ専用アプリ登録を用い他用途と共用しない**・**クライアントシークレットのローテーション手順**・**期限切れの検知は常設カードによる事後検知のみで、証明書のような事前警告（1017/1018）は存在しない**。⑤**流量上限による抑制が頻発する場合の報告先と手順**（常設カードの抑制表示は「保護動作であり通知が失われた可能性がある」旨と、報告先——GitHub Issue——への導線を含める。**再評価トリガ (a) が「利用者からの報告」を条件にする以上、報告経路を書かなければ (a) は装飾に過ぎない**）。⑥**廃止スケジュールの具体日は書かない**（「Microsoft 公式の最新案内を確認してください」の形にする。理由は改訂 1 の「日付をハードコードしない」方針） |

## 先送りにする場合の再評価トリガ

- **メール以外のチャネル（Slack / Teams / webhook）**: 独立した利用者からの要望 3 件以上で個別 ADR として再評価（決定 7 の ILogger レール上のシンク追加として実現できる。`Yagura.Abstractions` への公開契約の要否もその時点で判断）
- **トリガ選別・宛先振り分け**: 同じく要望 3 件以上で再評価。その際も「ログ内容に基づくルール」は NG 線（ADR-0001）のまま
- **implicit TLS（SMTPS 465）**: 対応が必要な利用者が現れた時点で `Security` の第 4 値として追加（構造変更を伴わない）
- **OAuth 2.0（Microsoft 365）**: **判定基準は「Basic 認証が廃止されたか」ではなく「M365 直送しか経路がない利用者が実際に送れるか」とする**（改訂 1 で置換。旧基準「廃止アナウンスの具体化または要望の反復」は、本トリガが守ろうとしている利益——主対象層が初版で置き去りにならないこと——を測れないため差し替えた）。該当時は**初版スコープ入りを再判断**する（「要望 3 件」を待たない——監視基盤を持たない現場ほど M365 直送しか経路がない可能性が高いため）
  - **判定基準を明確化する（改訂 1）**: 基準は「Basic 認証が廃止されたか」ではなく **「M365 直送しか経路がない利用者が実際に送れるか」** とする。前者は Microsoft の提供リストに載っているかを問うだけで、本トリガが守ろうとしている利益（主対象層が置き去りにならないこと）を測れない。
  - **2026-07-19 時点の判定: トリガ不発火（初版スコープ外を維持）。** ただし上記の実質条件は**一部で既に不成立**である。それでも維持するのは「まだ使えているから」ではなく、**OAuth を実装しても管理者側のセットアップ負荷が残り、主対象層にとっての利用可能性が実質改善しないため**である。調査の詳細・出典は**改訂履歴 1** を参照。
  - **再評価の契機**: (a) Exchange 側のセットアップが Web UI から完結できるようになった、(b) 送信専用に限定した権限（`FullAccess` 相当を要さない）が提供された、(c) M365 直送を必要とする利用者からの要望が反復した——のいずれか。
- **流量制御の閾値（決定 6 の仮値 6 点）の設定キー化**: 初版は**設定キーとして公開しない**（決定 6）。
  - **理由は「未実測の仮値に設定面を作るのが初版として時期尚早」であること**に限る。設定キーを 1 つ公開することは、キーの追加だけでなく**受理範囲の検証・§8 表への記載・テスト・そして以後の後方互換の負担**を一式引き受けることを意味する。値が実測（configuration.md §9 CF-x）で確定していない段階でこれを固めるのは早い。
  - **次の 2 つは非公開の根拠として使わない**（いずれも成立しないため。改訂 1 のペルソナレビューでの指摘）: ①「ゼロ設定ファーストランを損なう」——`Notification:Email:*` は opt-in で既定無効であり、ゼロ設定ファーストランには一切現れない（決定 1）。援用はカテゴリエラーである。②「`0` や過大値が事故経路になる」——決定 2 の不正値規則（機能無効化 + 警告）で既に塞がれており、二重計上になる。
  - **受容するコスト（明記）**: 閾値が現場に合わなかった利用者は、**次のリリースまで回避手段を持たない**。この不便を初版では受容する。
  - **再評価の条件**（いずれか 1 件で発火。他トリガの「要望 3 件」より低い閾値にするのは、抑制は利用者が気づきにくく報告の期待値が構造的に低いため）:
    - **(a) 流量上限による抑制が発生したという利用者からの報告 1 件**
    - **(b) 実測確定作業（configuration.md §9 CF-x）または開発側の常用環境での抑制の観測 1 件**——**報告に依存しない経路**として併置する。製品はテレメトリを持たないため、(a) だけでは「報告が届かないから永久に発火しない」状態になり得る（ADR-0019 の再評価トリガで同型の指摘を受けた経緯を踏まえる）
    - **(c) allowlist に「インスタンス多重度を持つ ID」が追加されるとき**——決定 6 が受容した EventId 粒度の畳み込みは、現在の allowlist（インスタンス数が高々数個）を前提にしている。[ADR-0018](0018-source-silence-detection.md) の送信元途絶検知のように**インスタンス数が送信元台数に比例する事象**が加わると、「1 台の途絶も 200 台の途絶も 60 分に 1 通・内容は最初の 1 件分」となり受容の前提が失われる。この場合は畳み込み粒度と流量閾値を同時に再評価する
  - 再評価時の候補は**全体流量上限のみを手編集キーとして公開する案**（下限・上限を製品側で固め `0` を受理しない。**UI へは出さない**）。**将来対応できる余地を残すこと自体は意図であり、初版で閉じているのは公開範囲であって設計ではない**

## 改訂履歴

### 1. 2026-07-19（amendment）: 委任 8 の実施結果を記録し、流量閾値を設定キー化しない理由を明文化

**契機**: 実装（Issue #350）着手前に委任 8（Exchange Online SMTP AUTH の検証）を前倒しで実施したため。PR #365。

**supersession ではなく amendment とした自己吟味**（`docs/adr/README.md` の判定の目安に従う）: 本改訂は**採用した選択肢の適用条件を追加・限定していない**。決定 2〜9 の既定（汎用 SMTP + STARTTLS + ユーザー名/パスワード、OAuth は初版スコープ外、流量閾値は非公開）はいずれも反転していない。最小の単位で検証しても、変わったのは**根拠の記述・調査結果の記録・受容コストの明示**であり、判断そのものではない。**決定以外の軸も検証する**（README の「最小の単位で反転の有無を検証」に従う）: 本改訂は**再評価トリガ（OAuth）の判定基準を置き換えている**。これは deferral の解除条件を緩めるものではない——旧基準「廃止アナウンスの具体化」では本トリガが守ろうとしている実質（主対象層が使えるか）を測れなかったため、測れる基準へ改めたうえで (a)(b)(c) の具体条件を新たに置いた。**放置防止の強度は下がっていない**（むしろ条件が具体化された）。また決定 5・6 には要件を追加しているが、いずれも既定を反転させず適用条件を具体化する側である。よって amendment。ヘッダーの日付は起案日（2026-07-18）のまま据える。

#### 1-A. 委任 8 の調査結果

**実施内容の限定**: 本調査は**公式ドキュメントの調査であり、実機で M365 へ SMTP 接続を試してはいない**（M365 テナントを要するため）。委任項目名は「ライブ検証」だが実施はこの範囲に留まる。実機確認は実装 PR 時に行う（委任 8 参照）。

| 確認事項 | 結果 | 出典（すべて 2026-07-19 確認） |
|---|---|---|
| SMTP AUTH（Basic）の現況 | **提供中**。"Although SMTP AUTH is currently available, Microsoft has announced plans to retire" | [Deprecation of Basic authentication in Exchange Online](https://learn.microsoft.com/en-us/exchange/clients-and-mobile-in-exchange-online/deprecation-of-basic-authentication-exchange-online)（ページ更新 2026-07-16） |
| 廃止スケジュール | 2026 年 12 月末に既存テナントで**既定オフ**（管理者が再有効化可能）。以降の新規テナントは既定で利用不可。**恒久削除日は未発表**（2027 年後半に「発表」予定） | [MC786329 の 2026-01-27 改訂タイムライン](https://jpmessaging.github.io/blog/Updated-Exchange-Online-SMTP-AUTH-Basic-Authentication-Deprecation-Timeline/)（Microsoft 日本 Exchange サポートチーム。2026-01-28 投稿 / 2026-01-30 更新） |
| **スケジュールは過去 2 回延期** | ①2025 年 9 月 → ②2026-04-30 → ③現行。**「2026 年 3 月開始 / 4 月末 100% 拒否」は撤回済みの旧情報**であり流通している | 同上 |
| すでに効いている制約 | 2020 年 1 月以降作成のテナントは**既定オフ**。Security Defaults 有効時は無効。条件付きアクセス / 認証ポリシーが上位で遮断 | [Enable or disable SMTP AUTH in Exchange Online](https://learn.microsoft.com/en-us/exchange/clients-and-mobile-in-exchange-online/authenticated-client-smtp-submission) |
| OAuth の無人サーバ対応 | **`SMTP.SendAsApp` による client credentials（対話ユーザー不要）が正式サポート**。scope は `https://outlook.office365.com/.default`。**GA / プレビューの別は確認できなかった** | [How to authenticate an IMAP, POP or SMTP connection using OAuth](https://learn.microsoft.com/en-us/exchange/client-developer/legacy-protocols/how-to-authenticate-an-imap-pop-smtp-application-by-using-oauth)（ページ更新 2025-10-17） |
| OAuth の管理者側負荷 | Exchange 側の `New-ServicePrincipal` / `Add-MailboxPermission -AccessRights FullAccess` が要る。**PowerShell 専用で Web UI から代行できない**。`FullAccess` は当該メールボックスの**全メール読み取り権限**を含み、送信専用の通知には過剰。**Application Access Policy / RBAC for Applications による限定可否は未検証**——上記は「Microsoft の公式手順がそう案内している」という事実であり、「限定手段が存在しない」ことの確認ではない | 同上 |
| ACS Email の SMTP 認証 | **クライアントシークレットをそのまま SMTP パスワードとして渡す**（"The password is one of the Microsoft Entra application's client secrets."）。XOAUTH2 の実装が不要 | [Send email with SMTP — authentication](https://learn.microsoft.com/en-us/azure/communication-services/quickstarts/email/send-email-smtp/smtp-authentication) |
| Microsoft の推奨代替 | "Send email to internal recipients only: Use High Volume Email... Send email to internal and external recipients: **Use Azure Communication Services Email**" | [Authenticated client SMTP submission](https://learn.microsoft.com/en-us/exchange/clients-and-mobile-in-exchange-online/authenticated-client-smtp-submission)（ページ更新 2026-06-01） |
| 業界全体の傾向か | **M365 固有**。オンプレ Exchange Server 2016/2019/SE は廃止告知の対象外（"listens for authenticated SMTP client submissions on port 587"）。Gmail はアプリパスワードが現役、SendGrid / Amazon SES は SMTP AUTH が第一級のインターフェース | [Configure Send connectors（Exchange SE）](https://learn.microsoft.com/en-us/exchange/mail-flow/connectors/) ほか |

#### 1-B. 他製品の対応方針（2026-07-19 調査）

本 ADR の判断が業界の常識から外れていないかを確かめるため、**自前サーバにインストールして使う型でメール通知機能を持つ製品**を調査した（すべて 2026-07-19 確認）。**選定基準**: ①本 ADR の直接/近接競合（Kiwi Syslog Server・Graylog）②同種の監視・ログ製品で採用実績の大きいもの（Zabbix・Grafana・Prometheus Alertmanager・Nagios・Icinga・PRTG）③無人サーバでメール送信を行う近接領域（Veeam・Synology・QNAP）。**クラウド SaaS 型の監視サービスは母集団に含めていない**（自前インストール型とは資格情報の扱いも管理者の権限も異なるため）。GitLab / Jenkins / Home Assistant / TrueNAS 等は**確認できなかった**。

| OAuth 対応 | 製品 |
|---|---|
| **非対応（7）** | Grafana・Prometheus Alertmanager・Graylog・Nagios Core・Icinga 2・PRTG・**Kiwi Syslog Server**（直接競合） |
| 対応（5） | Zabbix（7.4.0 / 2025-07-30）・Nagios XI・Veeam B&R（v12 / 2023-02）・Synology DSM・QNAP QTS |

この調査から得た 4 点を本 ADR の根拠に採り入れた。

1. **OAuth 非対応は多数派であり、明示的な設計判断として業界で通用している。** [PRTG は 2025-12-11 に「実装しない」と公式に結論](https://uservoice.paessler.com/forums/966003-notifications/suggestions/50095731-oauth-for-prtg-smtp-e-mail-delivery)を出し、代替として ACS をリレーに使うよう案内している。[Alertmanager は 2019 年の起票を「実装せず」でクローズ](https://github.com/prometheus/alertmanager/issues/1950)し、メンテナが「SMTP リレー / Webhook で回避せよ」と表明している。
2. **（参考情報）無人サーバ向けの OAuth を採っている製品は少ない。** 対応 5 製品のうち client credentials（無人）を採ったのは Nagios XI のみで、しかも **SMTP をやめて Graph API に切り替えた**実装。残る 4 製品はすべて**認可コードフロー = 管理者が一度ブラウザで同意**する方式であり、[QNAP は「認可が失効したら手動で再認可」が公式運用](https://docs.qnap.com/operating-system/qts/5.2.x/en-us/configuring-an-email-notification-server-EB4E6D7F.html)になっている。**ただしこれは「他社が採っていない」であって「採れない」ではない**（1-A のとおり技術的には `SMTP.SendAsApp` で可能）。Synology / QNAP / Veeam は家庭・中小の Gmail / Outlook.com 宛を主対象としており client credentials が適用しにくい領域である、という別の説明も同様に成立する。**したがって本項は 1-C の判定の柱には用いず、参考情報に留める**（第 2 巡のペルソナレビューで多数派論証であると指摘されたため降格）。判定の柱は 1-A の一次情報（PowerShell 専用手順・`FullAccess` の過剰権限）である。
3. **設定形の先例**: [Zabbix は `Authentication` = なし / ユーザー名+パスワード / OAuth の 3 択](https://www.zabbix.com/documentation/current/en/manual/config/notifications/media/email)を持つ。初版で OAuth を実装しなくても**選択肢の枠だけ先に切っておけば後から値を足すだけで済む**。逆に Grafana・Graylog・Kiwi のように「user と password が生えているだけ」だと設定体系ごと作り直しになる。**本 ADR はこの形に倣う**——この要求は改訂履歴の地の文に留めず、**委任 13 として委任表へ落とした**（実装者が改訂履歴を読まなくても届くように）。
4. **他製品も廃止日を自製品の文書に転記しており、その多くが現時点で実態とずれている**（確認できた範囲では Nagios・Paessler の記述）。ただし**「過去の日付が過去になる」のは同語反復であり、これ自体は 1-D の方針の根拠にはならない**——根拠は 1-A の「スケジュールが過去 2 回延期され、撤回済みの旧情報が流通している」で足りる。本項は参考の観察に留める（第 2 巡のペルソナレビューで指摘）。

**真似してはいけない点も明確になった**: 非対応 7 製品のうち Grafana・Graylog・Kiwi の 3 つは**利用者に何の案内もしていない**。**未対応であることより、逃げ道を示していないことのほうが批判される**——この認識から申し送り D-6 を新設した。

#### 1-C. 委任 8 の判定

**初版スコープ外を維持する（トリガ不発火）。ただし根拠は「提供中だから」ではない。**

判定基準を「Basic 認証が廃止されたか」ではなく **「M365 直送しか経路がない利用者が実際に送れるか」** とすると、この条件は**一部で既に不成立**である（上記の「すでに効いている制約」）。それでも維持するのは次の 2 点による。

1. **OAuth を実装しても主対象層の利用可能性が実質改善しない**——管理者側のセットアップ（PowerShell 専用手順・`FullAccess` の過剰権限）が残る。1-B の調査が示すとおり、**業界で無人サーバ向けの OAuth を解けている製品は事実上ない**
2. **塞がれている環境にも到達可能な代替経路が残っている**——ただし**経路ごとに前提条件が異なる**ため、以下に分けて記す（第 2 巡のペルソナレビューで、この 2 つを混同していた誤りを指摘された）。

| 経路 | Yagura 側 | 利用者側の前提条件 |
|---|---|---|
| **(i) 社内の SMTP サーバ（オンプレ Exchange 等）へ投げる**——**第一選択** | 「認証なしリレー」モードがそのまま使える | 社内サーバを **anonymous relay（open relay ではない）** に設定する。LAN 内の通信であり**グローバル IP は不要**。Microsoft 自身が「オンプレのメールサーバがあるなら M365 経由より**そちらを真剣に検討すべき**」「物理的に管理しているローカルのメールサーバの方が SMTP relay の設定は容易」と案内している |
| **(ii) M365 の SMTP relay コネクタ経由** | 同上（資格情報は要求されない） | **静的なグローバル IP アドレス、または accepted domain を含む TLS 証明書が必要**（"A certificate or static public IP address" / **"Dynamic IP addresses aren't supported or allowed"**）。ポート 25 が開いている必要があり（ISP が塞ぐことがある）、**Azure 等の第三者ホスティングからは利用できない** |
| **(iii) ISP・SendGrid / SES 等** | 汎用 SMTP（ユーザー名 + パスワード）がそのまま使える | 各サービスの契約 |
| **(iv) ACS Email** | 汎用 SMTP がそのまま使える | 下記の限定を参照（Azure 契約・DNS 検証等） |

**(ii) は「資格情報を要求しない」が「前提条件がない」ではない。** 静的グローバル IP を持たない小規模環境では成立しないことがあり、その場合の実質的な選択肢は (i)(iii)(iv) になる。

> 出典: [How to set up a multifunction device or application to send email using Microsoft 365 or Office 365](https://learn.microsoft.com/en-us/exchange/mail-flow-best-practices/how-to-set-up-a-multifunction-device-or-application-to-send-email-using-microsoft-365-or-office-365)（ページ更新 2026-06-01。2026-07-19 確認）。同ページは **Windows Server 2022 が SMTP server 機能（IIS 6.0）を持つ最後の版であり、M365 へのリレー用途では引き続きサポート対象外**とも述べているため、**IIS SMTP を手順書で推奨してはならない**（申し送り D-6）。

**ACS Email についての限定**（重要）: ACS は **Yagura 側の追加コード・追加依存・追加設定キーなしで接続できる**が、これは**製品側の対応が不要という意味であって、利用者の移行が容易という意味ではない**。利用者側には **Azure サブスクリプション契約（有料）・ACS / Email Communication Service リソースの作成・送信ドメインの DNS 検証（TXT / DKIM）・Entra アプリ登録**が新たに要求され、OAuth の PowerShell 手順を「導入容易性と衝突する」と評価したのと**同じ物差しでは軽くない**。また ACS は **M365 のメールボックスではなく別サービスへの移行**であり、「そのままの救済」ではない。

さらに **ACS 経路では `Smtp:Password` に格納される値の性質が変わる**——メールボックス 1 つの送信資格情報ではなく **Entra アプリ登録の資格情報**になる。DPAPI 保管（決定 3）は当該ホスト束縛にすぎないため、**syslog サーバの侵害から Entra アプリ資格情報への到達経路**が構成上生じる。加えて**クライアントシークレットは無期限にできない**ため、決定 5 が最も警戒する「チャネルの静かな死」が**確定イベントになる**。これらの注意は申し送り D-6 に落とした。

したがって結論は **「Yagura の実装形態は ACS を含む後継経路をそのまま収容できるため、廃止時に本 ADR の構造変更は要らない。ただし利用者側の移行コストは別問題であり、本 ADR はそれを解決しない」** に限定する。

#### 1-D. 「日付をハードコードしない」方針（適用範囲を明示）

廃止スケジュールが過去 2 回延期され、撤回済みの旧情報が流通している事実（1-A）と、日付を書いた他製品が例外なく陳腐化している事実（1-B）から、次を方針とする。

- **許可**: **本 ADR の改訂履歴（この節）に、取得日を明記したうえで**書く。ADR は履歴文書であり、**いつ時点の判断かが残ることは監査上むしろ必要**である
- **禁止**: `docs/design/` 配下の現在形文書・**利用者向けドキュメント本文**・コードの定数と条件分岐・テストの固定日・**UI の警告文言**
- **禁止対象は「提供終了・機能変更の*予定日*」に限る**。既に確定した過去のカットオフ日（例: 「2020 年 1 月以降に作成されたテナント」）は変動しないため対象外であり、利用者向け文書にも書いてよい
- **UI で伝えるのは日付ではなく症状と対処**（例:「このサーバは認証を受け付けませんでした。M365 をお使いの場合は D-6 を参照してください」）
- **強制点**: 申し送り D-6 と委任 12 に落とし、実装 PR / 文書作成時に参照される場所に置く

#### 1-E. 流量閾値を設定キー化しない理由の明文化（決定 6 の補足）

決定 6 は「設定キーとしては公開しない」と定めていたが**理由が書かれていなかった**ため、これを明文化した（再評価トリガ節を参照）。**決定は変えていない。**

明文化の過程で、当初案が挙げていた 2 つの理由——①「ゼロ設定ファーストランを損なう」②「`0` や過大値が事故経路になる」——は**いずれも成立しない**ことがペルソナレビューで指摘され、これを受け入れて撤回した。①は `Notification:Email:*` が opt-in で既定無効（決定 1）でありゼロ設定ファーストランには現れないためカテゴリエラー、②は決定 2 の不正値規則で既に塞がれており二重計上である。

**成立する理由は「未実測の仮値に設定面（受理範囲の検証・§8 表への記載・テスト・以後の後方互換）を作るのは初版として時期尚早」に限る。** あわせて**受容コスト**（閾値が現場に合わなかった利用者は次のリリースまで回避手段を持たない）を明記し、再評価トリガに**報告に依存しない経路**を併置した。

#### 1-F. 本改訂で追加・変更した箇所

| 箇所 | 内容 |
|---|---|
| 決定 5 | 常設カードの抑制表示を「累積回数のみ」から「**累積回数 + 直近の抑制時刻 + 抑制された EventId の内訳**」へ |
| 決定 6 | ①の実効上限の導出（エラーレベル対象外は無制限ではない）・全体上限が発動する条件・**allowlist の重大度を security.md §4.3 の表に列として持つ** |
| 帰結 | M365 の記述を「廃止方向・可能性がある」から「**既に使えない構成が現に存在する**」へ。**再有効化は推奨しない**という態度を明記。**MailKit の単一メンテナ依存**と退避可能性の評価を追加 |
| 委任 4 | 外向き通信台帳の宛先欄に「公衆網上の外部サービスになり得る」を明記 |
| 委任 8 | **完了**（文献調査の範囲）+ 実装 PR 時の実機確認を明記 |
| 委任 11（新設） | **抑制・流量制御の適用点**（enqueue / dequeue）の固定。決定 5 と決定 6 の衝突を実装 PR で解消させる |
| 委任 12（新設） | テスト送信・失敗表示の平易化（生の SMTP 応答だけを出さない） |
| 委任 13（新設） | 認証方式の設定形を、将来 `oauth2` を additive で追加できる選択肢の形にする |
| 申し送り D-6（新設） | M365 を送信先にする場合の案内（確認手順・代替経路の優先順位・再有効化を推奨しない態度・ACS の注意・**抑制頻発時の報告先**・日付を書かない） |
| 再評価トリガ | OAuth の判定基準を実質条件へ。流量閾値の再評価条件に **(b) 報告に依存しない観測** と **(c) allowlist へのインスタンス多重度 ID の追加** を追加 |

**ペルソナレビュー**: 5 ペルソナ（佐藤・鈴木・田中・リサ・クリス）が PR #365 でレビューを実施し、指摘を受けて本改訂の構造・根拠・出典を全面的に作り直した。対話は PR #365 のコメントに残る。
