# ADR-0021: フォワーダ MSI アップロードの前提条件の変更 — RequireForLoopback 必須からアップロード操作単位の実認証必須へ

- 状態: accepted（2026-07-26 マージ = PR #445。ペルソナレビュー round 1 = 5 件・変更部分再レビュー round 2 = 3 件を同 PR で実施）
- 日付: 2026-07-26
- 決定者: YANAI Taketo
- 関連: [ADR-0020](0020-forwarder-msi-upload.md)（決定 1 の有効化条件 (ii) = 本 ADR の部分 supersession 対象）/ [ADR-0010](0010-admin-ui-authentication.md)（決定 1 の loopback 認証 opt-in と復旧経路の代償）/ [ADR-0013](0013-admin-winauth-session.md)（Cookie/circuit ログインフロー——操作単位認可の実装基盤）/ [ADR-0011](0011-app-auth-failure-backoff.md)（失敗試行対策の loopback 除外）/ [ADR-0001](0001-project-founding.md)（「Windows 管理者が GUI で完結」の思想）/ [Issue #440](https://github.com/Yanai-Taketo/Yagura/issues/440)（起案元。lab 受け入れ初回実施の体験記録）/ Issue #283（ADR-0020 の再起案元。loopback 到達主体の実機棚卸し）/ [security.md](../design/security.md) §5.1.1 / [configuration.md](../design/configuration.md) §8

## 文脈と課題

### lab 受け入れ初回実施で観測された利用障壁（2026-07-26。Issue #440）

ADR-0020 決定 5 の lab 受け入れ検証を初回実施した際、フォワーダ MSI アップロード機能への
到達までに次の手順連鎖が必要だった:

1. 管理 UI 認証の有効化（GUI）
2. `Admin:Authentication:RequireForLoopback = true`（GUI。ただしロックアウトリスク警告つき）
3. ロックアウト対策の非常口アプリアカウント作成（パスワード 12 文字以上）
4. `Admin:ForwarderKit:MsiUpload:Enabled` の**設定ファイル手編集**（GUI なし）+ サービス再起動
5. `icacls` による ACE 付与（ターミナル）

GUI・設定ファイル・ターミナルの 3 操作面をまたぎ、「Windows 管理者が既に持つスキルだけで
導入・運用できる」というプロジェクト思想（ADR-0001）から乖離している。オーナーからも
「簡単に使えるを目標とした Yagura において複雑になりすぎている」
「ローカル 8515 ではアップロードを使えてもよいのでは（リモートは現行どおり）」という
問題提起があった（Issue #440）。

**透明性の注記**（ADR-0020 選択肢 A と同じ作法。ペルソナレビュー クリスの指摘）:
本起案はオーナーの問題提起（Issue #440）を起点とし、根拠は**第一者による lab 初回実施
1 回の体験（n=1）**である。独立利用者からの同種の報告は起案時点で 0 件。手数の重さ自体は
ADR-0020 帰結が「意図的な判断」として受容済みであり、lab 実施が新しい**事実**を追加した
わけではない——変わったのは「受容したコストの実際の体感」である。本 ADR はその体感を、
「(ii) の代償は本機能が必要とする保証（操作単位の身元保証）に対して過大」という
設計判断の再評価として扱う。

### 現行設計が (ii) RequireForLoopback を要求している理由の再確認

ADR-0020 決定 1 の条件 (ii) は「管理リスナに無認証の到達経路が存在しない」ことを構成で
保証するために置かれた。その動機は、無認証 loopback の暗黙プリンシパルでも既存の
管理ポリシー（`RequireAuthorization(AdminPolicy)`）が通ってしまうことにある——
無認証 loopback 構成では「認可済み = loopback から来た」でしかなく、
アップロード操作の主体を特定できない（ADR-0020 却下理由 2 の構造）。

つまり (ii) が守っているのは**アップロード操作の身元保証**であって、管理リスナ全体の
認証状態そのものではない。リスナ全体を守る手段（RequireForLoopback）で操作単位の要件を
満たしているため、本来不要な代償——ADR-0010 決定 1 の復旧経路の代償（認証手段全滅時の
復旧 = 設定ファイル手編集 + サービス再起動 = **syslog 受信断**）——まで輸入している。
ADR-0020 帰結はこの輸入を「知らずに交換させない」形で受容したが、lab 初回実施は
「知っていても重い」ことを示した。

### 脅威分析 — 「loopback なら無条件で許可」にできない理由

オーナー問題提起の直截な解釈（無認証 loopback ならアップロード可）は採れない。
ADR-0020 の実機棚卸し（Issue #283）が確定させたとおり:

- loopback 8515 に到達できる主体は、コンソールの管理者だけでなく**サーバ上の全ローカル
  プロセス・非管理者ユーザーを含む**（実測でサービスプロセス 93 個が到達可能。
  メンバーサーバでは既定で `BUILTIN\Users` = Domain Users 全員が対話ログオン権を持つ）
- `forwarder` フォルダの ACL は SYSTEM / Administrators / サービスアカウントのみ。
  非管理者ローカルユーザーは**直接には**書き込めない（security.md §5.1）
- 無認証 loopback でアップロードが使えると、書込 ACE を持つ**サービスプロセスが代行書き込み**
  するため、「非管理者ローカルプロセス → 全端末配布 MSI の差し替え」という**権限昇格経路**が
  開く。配布先では企業配布基盤経由で SYSTEM 権限インストールされ得る

よって「**無認証経路から MSI 書き込み口へ到達できない**」という ADR-0020 決定 1 の
fail-closed 不変条件そのものは維持しなければならない。変えるべきは不変条件ではなく、
その担保の単位——リスナ全体か、アップロード操作か——である。

## 検討した選択肢

### 選択肢 A: 現状維持（RequireForLoopback 必須のまま）

- 利点: 実装変更ゼロ。「管理リスナの全経路が認証済み」というより強い保証が既に成立している
- 欠点: 本機能のためだけに loopback 認証 opt-in とその復旧代償（受信断リスク・非常口
  アカウント運用）を輸入し続ける。操作面が GUI・設定ファイル・ターミナルの 3 面に分散した
  まま。lab 初回実施が示した利用障壁がそのまま残り、ADR-0020 決定 6 の効果測定
  （利用実態レビュー）が「使われない」側へ傾く要因を設計自身が作っている
- 却下（ただし本 ADR が却下された場合の受け皿として現行設計は成立し続ける）

### 選択肢 A': 現状維持 + 設定動線の GUI 化のみ消化（ペルソナレビュー クリスの質問による追加）

- (ii) 必須は維持したまま、ADR-0020 委任 1 の残課題（(i)〜(iii) の有効化が管理画面から
  完結する設定動線）だけを実装する案。lab 5 手順のうち手順 4（設定ファイル手編集）は
  これだけでも解消する
- 却下: 手順 2・3——(ii) の有効化と、その代償を受け止めるための非常口アカウント運用——は
  GUI 化しても消えない。**本 ADR が問題にしているのは手数の絶対量ではなく、周辺機能のために
  コア機能（受信）の可用性リスク（ロックアウト時の復旧 = 受信断）を輸入させる構造**であり、
  A' はそれに答えない。逆に言えば、本 ADR と A' の差分は正確に「(ii) の要否」であり、
  その当否は脅威分析（操作単位の身元保証で不変条件が維持できるか——決定 1 の
  資格情報発行口の統制を含む）で決まる

### 選択肢 B: 無認証 loopback でのアップロード許可（リモートのみ認証必須）

- オーナー問題提起の直截な解釈。利点は導入手数の最小化
- 却下: 上記の脅威分析のとおり、無認証 loopback の到達主体は「このホストでコードを実行
  できる者」全体であり、サービスの書込 ACE を踏み台にした権限昇格経路（非管理者ローカル
  プロセス → 全端末配布 MSI 差し替え）が開く。ADR-0020 決定 1 の不変条件の放棄であり、
  却下理由 2（操作者の身元保証の構造的不成立）の再来でもある。検討の俎上に載せない

### 選択肢 C: アップロード操作単位の実認証必須（採用）

守る対象を「リスナ全体」から「アップロード操作」へ絞る:

- アップロード系操作専用の認可ポリシーを新設し、「暗黙の loopback プリンシパルを拒否し、
  実際にサインインした管理者のみ許可」とする
- `RequireForLoopback = true` を前提条件から撤廃する（認証手段が最低 1 つ構成済みである
  ことは引き続き要求する）
- loopback の閲覧・通常管理は従来どおり無認証のまま（ゼロ設定思想を維持）。アップロードを
  使うときだけその場でサインインする（ADR-0013 のログインフローに乗る。Windows 統合認証
  なら追加入力なしで成立する**見込み**——断定しない。無認証で確立済みの circuit からの
  途中サインインの成立は未検証領域であり、実機検証を受け入れ基準に含める——委任 1）
- 利点: 不変条件（無認証経路から書き込み口へ到達不能）を**操作単位の認可で維持**しつつ、
  ロックアウトリスクの輸入と操作面の分散を解消する。監査の「誰が」欄も引き続き必ず実名が入る
- 欠点: 専用ポリシーという新しい実装・テスト面が増える。「同じ画面の中で認証状態が操作に
  よって異なる」という UI 状態が生まれる（帰結参照）

## 決定

選択肢 C を採用する。ADR-0020 決定 1 の有効化条件 (ii) を部分 supersede する。
それ以外の ADR-0020 の決定（決定 2 の ACL 開放統制・決定 3 の操作仕様・決定 4 の監査・
決定 5 の受け入れ基準・決定 6 の効果測定・決定 7 の supersession 範囲）は**すべて不変**である。

### 1. アップロード操作専用の認可ポリシー

- **対象操作**: stage（アップロード本文の受理）・commit・discard・delete、および
  アップロード用 antiforgery トークンの配布。すなわち配置フォルダへの書き込み・削除に
  つながる全操作と、その入口
- **判定**: 「実際にサインインした管理者」のみ許可する——Windows 統合認証
  （Negotiate/Kerberos）またはアプリ独自認証で認証済みのプリンシパルが管理役割
  （既定 `BUILTIN\Administrators` SID 判定・AD 管理グループ・アプリ独自アカウント——既存の
  管理認可判定そのまま）を保持していること。**無認証 loopback の暗黙プリンシパルは、
  管理ポリシーが通る構成であっても本ポリシーでは拒否する**
- **実装面**: HTTP エンドポイント（stage）は専用ポリシーの `RequireAuthorization` で守る。
  Blazor circuit 側の操作（commit / discard / delete）は circuit の認証状態
  （ADR-0013 の Cookie/circuit ログインフローで汲み直された認証済みプリンシパル）で
  同じ判定を行う。判定ロジックは HTTP 側・circuit 側で単一実装を共有する
  （検出/アップロードの版読み取りを単一ソースにしたのと同じ流儀）
- **資格情報の発行口も同じポリシーで統制する（ペルソナレビュー クリスの指摘 1 による追加。
  本 ADR の不変条件主張の成立条件）**: `RequireForLoopback = false` 構成では、認証設定の
  変更・**アプリ独自アカウントの作成/パスワード変更**が無認証 loopback の管理画面から
  実行できる（実装確認済み: `AdminAuthenticationAdminService.ConfigureAsync` は呼び出し元の
  認証状態・現行パスワードを検査しない——ADR-0010 決定 3 の設計どおり）。このままでは
  「無認証 UI でアプリアカウントを自己発行/上書き → その資格情報で『実認証』→ 本ポリシー
  通過」というブートストラップ迂回が成立し、操作単位認可は空洞化する。よって
  **(iii) が有効な構成では、認証設定の変更・アプリアカウントの作成/変更操作にも本ポリシーと
  同じ判定（実認証済みの管理者のみ）を適用する**。ブートストラップのデッドロックは
  生じない——(i) により認証手段が最低 1 つ構成済みであることが (iii) 有効化の前提であり、
  アプリ認証の有効化は設定検証がアカウントの同時設定を要求するため、サインイン経路が
  存在しない状態で本統制が発動することはない。(iii) 無効の環境では従来どおり
  （ADR-0010 決定 3 の bootstrap 設計は不変——本統制は MSI 書き込み口が存在する構成に限る）。
  なお (i) の認証手段が「構成済みだが使用不能」へ縮退した場合（Windows 認証のみ構成で
  SPN 障害等）、本機能の復旧経路は認証の緊急無効化（手編集 + 再起動——(iii) の同時無効化を
  含む。委任 3 の手順書が受け皿）である（round 2 クリスの指摘による参照明示）
- **(iii) 有効化前に発行された資格情報（事前仕込み）への対処（round 2 田中・クリスの指摘に
  よる追加）**: 発行口統制は (iii) 有効時に限るため、「(iii) 無効の間に無認証 loopback で
  アプリアカウントを自己発行/上書きしておき、後日 (iii) が有効化された後にその資格情報で
  サインインする」という時間差の迂回が理論上残る（仕込み時の監査は接続元しか残らない——
  ADR-0010 決定 6 の既定 loopback の限界）。統制の常時適用は ADR-0010 決定 3 の bootstrap を
  壊すため採らず、**切替時点検で対処する**: (iii) の有効化動線（委任 2 のトグル・1032 起動
  検証を通る手編集の双方）で、**既存アプリアカウントの有無・最終変更時刻を提示し、管理者に
  そのアカウントが自分の管理下にあることの確認を求める**（トグルでは確認チェック、起動検証
  経路ではイベントログへの情報記録）。確認後も残る偽装の可能性（管理者が攻撃者の仕込みを
  自分のものと誤認する）は、アカウント作成・変更の監査記録（作成時刻・接続元）との突合で
  検出可能な残余リスクとして受容し、帰結に明記する
- **ポリシー拒否の監査（ペルソナレビュー 田中・鈴木の指摘による追加）**: 本ポリシーによる
  認可拒否（無認証 loopback 主体によるアップロード系操作・資格情報発行操作の試行）は
  ADR-0004 決定 7「拒否された試行も監査対象」に従い 3000 番台へ記録する（3014 への統合か
  新番かは実装 PR で確定——委任 1）。この拒否は本 ADR が新設した権限昇格試行のシグナル
  そのものであり、「拒否されること」だけでなく「拒否が見えること」を設計条件とする
- **閲覧・通常管理への影響なし**: 本ポリシーは上記の対象操作にのみ適用する。
  loopback の無認証閲覧・通常管理操作（`RequireForLoopback = false` 構成）は従来どおり

### 2. 有効化条件の改訂（ADR-0020 決定 1 の条件 (ii) の撤廃）

アップロード機能（配置経路 (b)）の有効化条件を次の 2 条件へ改める:

- **(i) 管理 UI 認証が有効**: Windows 統合認証・アプリ独自認証の少なくとも一方が有効
  （不変。サインインの手段が存在しなければ操作単位認可は誰も通過できないため、
  引き続き前提条件である）
- **(iii) アップロード機能自体の明示 opt-in**: `Admin:ForwarderKit:MsiUpload:Enabled`
  （不変。既定 `false`）

**(ii) `Admin:Authentication:RequireForLoopback = true` は前提条件から撤廃する**。
`RequireForLoopback` 自体の機能・意味（ADR-0010 決定 1）は一切変更しない——管理リスナ
全体を認証必須にしたい環境は引き続き有効化でき、その構成でも本機能は当然に動作する。

付随する改訂:

- **fail-closed（起動時。イベント ID 1032）**: 拒否条件を「(iii) 有効 + (i) 不成立」へ
  変更する。エラーメッセージの復旧誘導（本キーを `false` に戻す）は不変
- **fail-closed（UI）**: 逆方向の対称防御も (i)(iii) の 2 条件へ追随する（(iii) 有効の
  稼働構成で (i) を無効化する設定変更は書き込み前に UI レベルで拒否——不変条件の維持）
- **エンドポイントの構造的非存在**（条件不成立時は登録自体を省略。無効構成では 404）は
  判定条件が (i)(iii) に変わるのみで機構は不変
- **UI 表出**: 生成画面の条件案内から (ii) 関連——RequireForLoopback の有効化案内・
  ロックアウト代償の提示・ブレークグラス強推奨——を**本機能の文脈からは**除去する
  （ADR-0010 決定 1 の代償の記述自体は認証設定画面の管轄として残る）。代わりに、
  (i)(iii) 成立 + 未サインインの状態では「アップロード操作にはサインインが必要」の案内と
  サインイン動線を表示する（条件不成立時に沈黙にしない流儀は不変）

### 3. 不変条件の維持宣言と ADR-0011 整合

- **「無認証経路から MSI 書き込み口へ到達できない」は維持される**: 担保が
  「リスナ全体の認証必須（構成レベル）」から「アップロード操作の実認証必須（操作レベル）」へ
  移るだけで、無認証の主体が書き込み口に到達できない事実は変わらない。ADR-0020 の
  却下理由 2（操作者特定の構造的不成立）への回答——監査 2026/2027 の「誰が」欄に必ず
  認証済み利用者名が入る——も維持される（本ポリシーを通過した操作は定義上すべて認証済み）
- **ADR-0011 決定 4（loopback は IP レート制限・グローバルトークンバケットの対象外）は
  不変**: (ii) 撤廃により「loopback からアプリ独自認証への低速試行が③バックオフのみに
  律速される」露出の議論（ADR-0020 決定 1）は前提ごと変化するが、受容の構造は同じ——
  ローカル実行主体による試行は監査（3004/3006）・1019 昇格で観測される。この露出を持つ
  環境の増減は両方向がありうる（ペルソナレビュー 鈴木の指摘による中立化）——本 ADR は
  RequireForLoopback の有効化を促さない一方、「アップロードのために (i)（アプリ独自認証を
  含む）を新規構成する環境」を増やす方向でもある。受容の根拠は観測可能性（監査・1019）で
  あって環境数の増減ではない
- **ADR-0020 決定 2（ACL 開放は OS 管理者の明示 `icacls` 操作のみ）は不変**: 書き込みの
  物理的成立には引き続き ACE 付与が必要であり、「使うときだけ開く」運用・二系統の ACL 検出
  （1033/1034）・開放状態の常時表示はそのまま適用される。操作面の分散は 3 面 → 2 面
  （GUI + ターミナル）に減るが、ACE 付与のターミナル操作は本 ADR の対象外として維持する
  （書き込み口の物理的開閉を OS レベルの身元保証に残す構造は B-2 採用の核心であり、
  ここを UI 化すると ADR-0020 選択肢 B-2 の却下理由が再来する）

### 4. 設定動線の改善（(iii) の GUI トグル化）

lab 初回実施の手順 4（設定ファイル手編集 + 再起動）も操作面分散の一因である。
`Admin:ForwarderKit:MsiUpload:Enabled` の管理画面トグル化（1032 の fail-closed 検証は
UI 書き込み前判定として維持）を**本改訂の実装と同時に行うことを原則とする**
（ペルソナレビュー 佐藤の指摘により「検討」から格上げ。見送る場合は理由の記録に加え、
改訂後の全手順を lab で再実測して手数を記録することを条件とする——決定 5 の流儀）。
ADR-0020 委任 1 の「(i)〜(iii) の有効化が管理画面から完結する設定動線」の残課題を、
条件が 2 つに減った本改訂の下で消化する位置づけである。

**トグル操作の認可はアップロード操作と同格とする**（ペルソナレビュー 田中・リサ・クリスの
指摘の収束点）: (iii) の有効化・無効化は「MSI 書き込み口を出現させる/消す」操作であり、
決定 1 の専用ポリシーと同じ判定（実認証済みの管理者のみ）を要求する。無認証 loopback から
opt-in を反転できてはならない。

**有効化には再起動が必要である事実を隠さない**（ペルソナレビュー 佐藤・リサの指摘）:
(iii) の反映方式は再起動（エンドポイントの構造的非存在の判定が起動時固定）であり、
**機能を有効化する時点で受信断の窓（サービス再起動）が 1 回必要になる**。トグル UI は
この代償を明示する（configuration.md §3 の再起動待ち表示に乗る）。動的反映（再起動なしの
エンドポイント出現）は「構造的非存在」の保証を弱めるため採らない——404 を返す口すら
無い状態は、起動時に確定しているからこそ検証可能である。

## 帰結

- 良くなること:
  - **ロックアウトリスクの輸入が消える**: 本機能のために `RequireForLoopback` を有効化する
    必要がなくなり、ADR-0010 決定 1 の復旧代償（受信断）・非常口アカウント運用を
    本機能の利用者が背負わなくなる
  - **操作面の分散が減る**: RequireForLoopback 関連の手順（lab 初回実施の手順 2・3）が
    消滅する。委任 2（GUI トグル化）が成立すれば、残るターミナル操作は ACE 付与のみになる
  - **監査の「誰が」欄は常に実名のまま**（不変条件の維持）。サインインの時点が操作に
    近づく傾向はあるが、「その操作のために認証した」とまでは証跡上主張しない——既存
    セッションの残存中は後日の操作も同じセッションに乗るため、この主張の成立には
    セッション鮮度（再認証窓）の規定が要る（ペルソナレビュー 田中の指摘。鮮度要件を
    設けるか否か・セッション有効期限との関係は委任 1 で確定する）
  - リモート経路（8516。認証 + HTTPS 必須）は変更なし
- 悪くなること（受け入れるトレードオフ）:
  - **専用認可ポリシーという新しい実装・テスト面が増える**: 「暗黙 loopback プリンシパルの
    拒否」が HTTP 側・circuit 側の両方で正しく効くことの回帰テスト（決定 5 相当の受け入れ
    基準——委任 1）が必須になる。判定の単一実装共有はその緩和である
  - **同一画面内の認証状態の非対称**: 無認証 loopback で生成画面を開けるが、アップロード節
    だけはサインインを要する。「なぜこの操作だけ」の説明責務が UI に生じる（決定 2 の
    案内文言で対処。委任 1）。初見では故障と区別がつかない恐れがあるため、案内文言は
    ADR-0020 委任 4 と同じく文言一覧またはスクリーンショットを実装 PR のレビュー対象として
    提示する（ペルソナレビュー 佐藤の指摘）
  - **(iii) の有効化には再起動 = 受信断の窓が 1 回必要**（決定 4）: 本改訂はロックアウト
    起因の受信断リスクを消すが、機能有効化時の計画的な再起動は残る。計画的・1 回限りで
    ある点がロックアウト（無計画・復旧まで継続）とは質的に異なるため受け入れる
  - **無認証 loopback の管理面そのものは本 ADR の射程外として残る**: (iii) 無効の環境では
    認証設定・アプリアカウント操作が無認証 loopback から可能という ADR-0010 決定 3 の
    bootstrap 設計は不変である。本 ADR が閉じるのは「その面から MSI 書き込み口へ至る経路」
    のみ（決定 1 の資格情報発行口の統制は (iii) 有効時に限る）
  - ADR-0020 が持っていた「管理リスナ全体が認証済み」という**より強い保証は、本機能の
    前提としては**失われる（RequireForLoopback を自ら有効化する環境では引き続き成立する）。
    本機能が必要とするのは操作単位の保証であることを本 ADR の脅威分析で確認済みであり、
    リスナ全体の認証はそれ自体の判断として ADR-0010 の管轄に戻る
- 受け入れるリスク:
  - **(iii) 有効化前に無認証 loopback で仕込まれたアプリアカウントによる偽装**（決定 1 の
    切替時点検で緩和）: 点検の提示（既存アカウントの有無・最終変更時刻）を管理者が誤認
    すれば迂回は成立しうる。アカウント作成・変更の監査記録との突合で事後検出可能である
    ことをもって受容する（round 2 田中・クリスの指摘による明記）
  - プロセス侵害シナリオ（ADR-0020 帰結）の残余リスクは本改訂で増減しない——認証・監査を
    プロセス自身が握る限界も、SACL・外部コピーが実効的な独立証跡である事実も不変
  - Windows 統合認証が構成できない・アプリ独自認証のパスワード運用を避けたい環境にとって、
    (i) が引き続き参入条件になる。これは意図的である——全端末に配布される MSI の差し替え口
    に身元保証なしの経路を作らない（選択肢 B の却下理由）

## ADR-0020 決定 6（効果測定）との関係

本改訂は利用障壁を下げる変更であり、ADR-0020 決定 6 の利用実態レビュー（v1.0 公開後
6 ヶ月以内目安）は**本改訂の実装がリリースに載った後の実態**で評価するのが公平である
（Issue #440 の整理のとおり）。撤回基準・期限の枠組み自体は変更しない。

## 先送りにする場合の再評価トリガ

本 ADR が却下（選択肢 A 維持）となった場合、以下のいずれかで再評価する:

- ADR-0020 決定 6 の利用実態レビューで「前提条件の重さ」が不使用の主因として観測された場合
- RequireForLoopback 有効化に起因するロックアウト・受信断の実例が報告された場合
- 閲覧 UI 認証を含む認証運用が既定化する方向の再評価（ADR-0004 の既存トリガ）が成立した場合

## 委任事項の一覧（追跡用）

| # | 委任事項 | 委任先 | 内容 |
|---|---|---|---|
| 1 | 専用認可ポリシーの実装と受け入れ基準 | 実装 PR + security.md §5.1.1 | ポリシーの実装（HTTP 側 `RequireAuthorization` + circuit 側判定の単一実装共有）。**回帰テスト: ①無認証 loopback の暗黙プリンシパルで stage/commit/discard/delete と antiforgery 配布の全操作が拒否されること、②認証済み管理者（Windows/アプリ独自の両方式）で通過すること、③(iii) 有効 + (i) 不成立の 1032 起動拒否の条件改訂の追随、④(iii) 有効構成で「無認証 loopback からの資格情報自己発行（認証設定変更・アプリアカウント作成/変更）→ サインイン → ポリシー通過」の迂回が成立しないこと（クリスの指摘 1 の否定シナリオ）、⑤(iii) 無効時に仕込んだアカウントが (iii) 有効化動線の切替時点検（既存アカウントの提示・確認）に必ず表示されること（round 2 田中・クリスの事前仕込み経路の検証）**。**ポリシー拒否の監査採番（3014 統合か新番か）と記録フィールド**。**セッション鮮度（アップロード操作に再認証窓を設けるか否か）の確定**（田中の指摘）。**stage（HTTP）通過後に commit（circuit）時点でセッションが失効していた場合の失敗様式——ステージングは孤児掃除に乗ること・利用者表示・監査記録——の確定**（鈴木の質問 2）。未サインイン時の案内文言とサインイン動線（「なぜこの操作だけ認証が要るか」の平易な説明を含む。**文言一覧またはスクリーンショットをレビュー対象として提示**——ADR-0020 委任 4 の流儀）。**実機検証を受け入れ基準に含める: 無認証確立済み circuit からの途中サインイン（step-up）の成立と、antiforgery トークン配布を認可対象に含める方式の実機確認**（クリスの指摘 2——「見込み」を実測で確定させるまで accepted 後も実装をリリースに載せない）。lab 受け入れ項目は ADR-0020 決定 5 の②〜④を本改訂後の構成で消化する |
| 2 | (iii) の GUI トグル化 | 実装 PR（本改訂と同時が原則——決定 4） + configuration.md | `Admin:ForwarderKit:MsiUpload:Enabled` の管理画面トグル。**トグル操作の認可は決定 1 の専用ポリシーと同一判定**（無認証 loopback から opt-in を反転できてはならない——田中・リサ・クリスの指摘）。UI 書き込み前の fail-closed 判定（1032 と同条件）維持。**有効化時の切替時点検——既存アプリアカウントの有無・最終変更時刻の提示と管理者の確認（決定 1 の事前仕込み対処）——をトグル動線に組み込む**（手編集 + 再起動の経路では 1032 起動検証時にイベントログへ情報記録）。**反映は再起動（受信断の窓 1 回）である事実の明示**（決定 4）。見送る場合は理由の記録 + 改訂後全手順の lab 再実測（決定 4） |
| 3 | 設計書・ガイドの追従 | 実装 PR | security.md §5.1.1（前提条件・認可ポリシー・資格情報発行口の統制の記述）・configuration.md §8（`Admin:ForwarderKit:MsiUpload:Enabled` の不正時挙動欄の前提条件）・利用者ガイド（RequireForLoopback 前提の記述の除去・サインイン動線の記述・**改訂前後の手順比較表**——佐藤の指摘）・`ForwarderKitScreen` の条件案内文言。ADR-0020 委任 9 の「(ii) 新規有効化時の代償とブレークグラス強推奨」は本機能の文脈から除去する（認証設定画面の一般記述としては維持）。**同委任 9 の「認証の緊急無効化手順（手編集）に『(iii) も同時に無効化する』を追記」は本改訂後も維持する**（鈴木の指摘——(iii) 有効 + (i) 不成立は引き続き起動拒否のため、緊急無効化時の受信断延伸経路は残る）。**ADR-0010 決定 9 の「RequireForLoopback 有効化を前提条件とすることを強く推奨」の指針に本 ADR への参照注記を加える**（リサの指摘——矛盾したまま残さない） |

## 実機検証の記録（委任 1 の受け入れ基準。2026-07-26）

クリスの指摘 2 により「見込み」として起案し、実測確定まで機能をリリースに載せないと定めた
項目（無認証で確立済みの circuit からの途中サインイン = step-up の成立、antiforgery トークン
配布を認可対象に含める方式の成立）を実測した。**結果はいずれも成立**。

- **環境**: Windows Server 2025 (10.0.26100)。実装済み main のローカル実行（隔離データルート・
  管理リスナ 58515。インストール済みサービスには触れない構成）。ブラウザは実 Blazor circuit
- **① 未サインイン（無認証 loopback）**: 生成画面のアップロード区画にトグルも操作 UI も現れず、
  「この設定の変更にはサインインが必要です」+ サインイン動線のみが表示された。
  `GET …/msi/antiforgery` は **401**（`X-Requested-With` 付き）/ **302 → /admin/login**（ブラウザ遷移）、
  `POST …/msi/stage` は **401**。いずれも監査 **3014 `reason=operation-auth-required
  connection=loopback`** が記録された（「拒否が見えること」の実測）。一方 `/admin`（通常管理）は
  **200** のまま——ゼロ設定思想の維持も同時に確認
- **② step-up サインインの成立**: 同一ブラウザで `/admin/login` からアプリ独自認証でサインイン
  （フルページ遷移 → `/admin`）した後、生成画面を開くと**アップロード区画の操作 UI が現れた**。
  無認証で確立済みの circuit が認証状態を獲得できない、という懸念（ADR-0010 検証 3 / Issue #252
  と同根の未検証領域）は生じなかった
- **③ 前提条件の改訂（決定 2）**: `Admin:Authentication:RequireForLoopback = false` かつ
  `Admin:ForwarderKit:MsiUpload:Enabled = true` の構成で**起動が成立**（1032 は発生しない）。
  旧仕様では起動拒否だった構成である
- **④ 切替時点検（決定 1・委任 2）**: 有効化トグルが既存アプリアカウントの
  「ユーザー名 / 作成 / 最終変更」を**実時刻付きで提示**（スキーマ v3 の列が UI まで通っていること
  の確認）。確認チェックなしの有効化は拒否され、確認後の保存で監査 2001 に
  `existingAppAccount=… inventoryAcknowledged=True` と操作者実名が記録された
- **⑤ 正常系アップロード（ADR-0020 決定 5 lab ②ケース A。Issue #436 で阻止されていた経路）**:
  認証済みセッションで antiforgery トークンを取得 →`fluent-bit-5.0.8-win64.msi`（25,360,999 バイト）を
  stage し、**`productVersion=5.0.8`・`officialHashMatch=Match`** で受理された。ステージングは
  検出パターン非一致の `.uploading-<GUID>.msi` として配置フォルダ内に作られることも確認

**残る lab 項目**: 実インストール環境での ACE 付与・撤去（ADR-0020 決定 5 lab ①）、ACL 検出の
二系統（同 ③）、アカウント切替（同 ④）は本記録の対象外（インストーラ経由の構成が必要）。
本記録が確定させたのは委任 1 が名指しした「未検証領域」と、それに連なる正常系の成立である。

## 改訂履歴

（初回 amendment 発生時にこのセクションへ追記する）
